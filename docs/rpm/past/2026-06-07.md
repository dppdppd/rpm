# 2026-06-07

## Accomplished
- **Dissuaded external/manual looping in favor of interior `/next` sequencing — shipped v2.25.0.**
  Now that `/next` has real interior sequencing (v2.24.0), reframed the skill copy so the
  sequence forms (`/next N | blocked | all | <group>`) are the recommended way to work several
  items at once; `/loop /next` is kept but described as the tool for genuinely unattended,
  multi-turn runs. Touched `next/SKILL.md` (description, opening paragraph, usage block),
  `rpm/SKILL.md` (`/next` row), `log-decision.sh`, and `translate-skill-codex.py`.
- **Added a one-time "Cheaper-path tip."** On a Loop-mode tick with ≥2 actionable tasks and no
  prior `nudge` marker since the last `loop-exhausted`, `/next` appends one line
  (`tip: /next all runs this in one cheaper turn`) and logs an ignored `nudge` marker. Fires at
  most once per loop run; never blocks, never repeats. User chose this "reframe + one-time nudge"
  level over docs-only or an every-tick warning.
- **Fixed a latent Codex leak.** The `rpm` skill's `/loop /next` literal was passing untranslated
  into the Codex port; reworded that row runtime-neutral so it no longer leaks.
- **Published v2.25.0** to all three runtimes (CC `master`, opencode, codex) + tag `v2.25.0`;
  dev monorepo pushed. Dry-run first, then real publish. bats 212/212, shellcheck clean.

## Key Discoveries
- `translate-skill-codex.py`'s loop / loop-mode catch-alls live inside `codex_next_body`, which
  only runs for `skill_name == "next"` — so `/loop /next` and "loop mode" phrasing in *other*
  skills leaks untranslated. Write looping references runtime-neutral in non-`next` skills.
  (Captured in `feedback_codex_translator_fragile`.)
- Many of the translator's exact-sentence replacements are dead no-ops after the v2.24.0
  restructure; the robust token-level catch-alls are what actually keep the Codex `next` skill
  correct today.

## What Didn't Work
- Nothing notable — clean, single-deliverable session.

---

## Session 2 — native arm + finalize the v1│v2│native VOC bake-off

### Accomplished
- **Ran the native arm of the Dutch-primary VOC bake-off** via the bundled
  `/deep-research` skill (a native Claude Code Workflow — Scope → Search → Verify →
  Synthesize, 3-vote adversarial verification). Gave it ONLY the verbatim question
  (no key, no corpus, no bake-off framing) so it was graded on the same footing as
  v1/v2. Scale: 103 agents · ~2.32M subagent tokens · 757 tool-uses · ~27 min.
  Persisted to `runs/2026-06-07-triplet/native/report.md`.
- **Graded native blind** with an independent Dutch-fidelity subagent (read the
  held-out key + frozen corpus, re-translated the Dutch itself) →
  `grading/native-grading.md`. Native swept: Sub-Q1/Q2a/Q3 all CORRECT, 0
  translation errors, 0/10 unsupported, 0 figure orphans, 0 over-kill, live-URL
  17/18; Sub-Q2b left explicitly unresolved (surfaced both readings, refused to
  assert).
- **Finalized the 3-column scorecard** (`scorecard.md`), appended a `## Worker
  Result` to the backlog detail file, marked `dr-v1-v2-native-dutch-bakeoff` DONE,
  updated `status.md`.

### Key Discoveries
- **Native is the strongest arm on Dutch-source fidelity — but at ~20× the token
  cost** (~2.32M vs v1 ~98k / v2 ~115k). It is the only arm that delivered no wrong
  answer on Sub-Q2.
- **The v1→v2 hardening's real verdict: no fidelity gain, one false-confidence
  regression.** v2's kill-list/figure-ledger neither beat v1 nor caught the Sub-Q2
  wrong-referent trap — it certified a fabricated officer as HIGH-confidence /
  zero-orphan. The mechanism that *actually* caught the traps is native's
  **independent adversarial multi-vote verification** (refute-by-majority), not
  literal-presence self-certification. Lesson: reserve adversarial verification for
  high-stakes primary-source claims; it's expensive. n=1 probe — directional.
- The bundled `/deep-research` skill registered this session is a genuinely distinct
  native Workflow harness (description "fan-out … synthesize a cited report"), NOT
  rpm's own `research` skill nor its old cached `deep-research` — confirmed by
  comparing frontmatter descriptions before invoking.

### What Didn't Work
- Nothing notable — the native run completed within budget in a fresh session, as
  the hand-off TODO anticipated.

## Next
- `injection-weak-model-arm` — run Module B canaries on a non-Claude weak model
  (portability). Needs a non-Claude runtime. (Now top actionable backlog item.)

### 20:16 pre-compaction checkpoint
- **Task:** (unassigned)
- **Modified files (2):**
  - codex/.codex/skills/research/SKILL.md
  - plugin/skills/research/SKILL.md
- **Auto-captured learnings:**
  - he bake-off tells us is **counterintuitive: v2 already contains the lenses that should have caught this.** Look at what's in `research/SKILL.md` today (Phase 4): - a **cross-source / internal-consistency lens** (line 229) — exactly what should have flagged "De Ros vs Vogel"
  - ## Why this changes the fix (and it's the more interesting finding)
