# Commit, push, and release current changes

## Request
Commit the current work, push it, publish the plugin ports, and verify the release.

## Notes
- Started from one local commit ahead of `dev/master` plus Codex hook/test changes.
- Keep the release workflow scoped to the current tree and verify CI after publishing.
