# rpm-opencode (prototype)

Port of rpm to [opencode](https://opencode.ai). Prototype status — the
event→bash-hook bridge is proven end-to-end (see
`docs/rpm/future/2026-04-14-opencode-port.md`), but distribution and
agent translation are still open.

## Layout

- `opencode.json` — opencode config for this dir (mostly empty; opencode
  auto-discovers everything under `.opencode/`)
- `.opencode/plugins/rpm.ts` — the plugin (source of truth)
- `.opencode/skills/` — **gitignored**, regenerated by
  `scripts/sync-opencode.sh` from `plugin/skills/`
- `.opencode/agents/` — **gitignored**, regenerated from
  `plugin/agents/` via `scripts/translate-agent.py` (tools list →
  record, drops `name:`/`model:`, adds `mode: subagent`)

## Working on the prototype

```
# after editing any plugin/skills/** file
../scripts/sync-opencode.sh

# smoke test — list skills opencode can see
cd opencode && opencode debug skill | grep '"name"'

# full end-to-end (session lifecycle → bash hooks → docs/rpm/ mutation):
# run opencode serve against a project that has docs/rpm/ initialized
```

## Known gaps

- **Distribution** — whether to ship as an npm package or a
  subtree-split `opencode-only` branch is undecided. Both currently
  bundle `plugin/hooks/` relative to the plugin file.
- **First-session `session.created`** — missed because plugins load
  lazily. Worked around by running SessionStart logic inline in the
  Plugin init function; `session.updated` has not been tested as a
  secondary trigger.
- **`session.deleted` vs real exits** — only verified via explicit
  `DELETE /session/<id>`. Still untested on `/exit`, terminal close,
  kill.
