# 2026-05-30


### 12:31 session ended without wrap-up
- **Session:** e75587b2-383e-4efc-9650-ec4be6561b99
- **Task:** (unassigned)
- **Reason:** prompt_input_exit
- **Uncommitted files:** 0
- **Action:** next session should backfill past/ and commit outstanding work

### 19:41 pre-compaction checkpoint
- **Task:** (unassigned)
- **Modified files (4):**
  - docs/rpm/future/tasks.org
  - plugin/hooks/_directives.sh
  - plugin/skills/deep-research/SKILL.md
  - plugin/skills/next/SKILL.md
- **Auto-captured learnings:**
  - Key finding: there are **three** deep-research surfaces on this machine, not two — and the d

### Session — deep-research evaluation, hardening + eval harness

**Accomplished**
- Evaluated rpm in light of ultracode/Workflow → Tier-1/2 directive + cap guardrails (`_directives.sh`, `next/SKILL.md`).
- Controlled bake-off (`docs/rpm/research/dr-bakeoff/`): rpm:deep-research vs native `/deep-research` on a VOC-decline probe, blind-graded with citation-fetch. **Native matched/edged rpm on rigor.**
- Hardened `deep-research/SKILL.md`: number-provenance gate, kill-list + "Could not verify / refuted" section, quantitative source-tiering, active-refutation + default-to-kill verify (closes the citation gaps; max-4 cap untouched).
- Tier-1 offline auditor (`checks/offline_audit.py`) — validated vs the blind grader; **56% orphan-figure baseline** on figure-dense reports, ~0 research tokens.
- Tier-2 replay on voc-decline cached sources: hardened pass **kills ƒ134M + ƒ120M + 6 more (8/11)**, producing native's refuse-to-assert stance → fix validated.
- Commits `2ba5fcc` + `f5ab7fa`. Plan + backlog (steps 3–5 remain).

**Key discoveries**
- Native `/deep-research` = 5-angle → fetch-15 → 3-vote-refute-to-kill → synthesize Workflow (101 agents but near-serial; quality is from **process, not parallelism**).
- **rpm's edge is orchestration** (portability / durable artifacts / inline / cost) — NOT output rigor.
- Native's own gaps: no injection defense on fetched content, silent claim truncation (47/72 dropped), unchecked quote provenance, correlated voters/over-kill, synthesis drift.
- Citation-discipline failure is **pervasive (≥56% orphan), not n=1**.

**What didn't work**
- Overclaimed "rpm already covers native's gaps" — credited prose as behavior; user corrected.
- First bake-off attempt: both arms stalled pre-synthesis (interrupted); recovered from transcripts + re-ran native.
- Deterministic audit unreliable for 2-digit figures (coincidental digit matches) — ≥3-digit/decimal only.

**Next**
- deep-research verification-chain hardening (log silent drops + provenance gate + post-synthesis audit) — top of backlog.
- Then diverse-verify + Module B injection. A live re-run (to prove prose induces behavior) still deferred for tokens.
