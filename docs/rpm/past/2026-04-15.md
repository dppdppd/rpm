# 2026-04-15

*Backfilled 2026-04-17 from git log + status.md bullets. Session
`a4ee0e19` continued through this day and exited unwrapped on
2026-04-16.*

### Session A — decouple native tasks from backlog (v2.7.1)

Commits (09:14–19:06 local):

- `1ebf0a9` decouple `tasks.org` adds from native `TaskCreate`
- `ac0232d` carry "Key finding:" guidance into resume + post-compact
- `adbd3af` backlog — port rpm to opencode (detail file)
- `264458c` backlog — port rpm to Codex CLI (detail file)
- `8eb14c9` bump plugin.json + status.md to v2.7.1

`/tasks add` now explicitly forbids `TaskCreate` for backlog
additions — native tasks (`TaskCreate`/`TaskList`) are
current-session only; `tasks.org` is the long-term backlog.
Session-start injection states the distinction so Claude doesn't
double-list.

"Key finding:" directive now fires across all three session
injection paths — startup, resume, post-compact. Previously it
was only in startup, so resumed / post-compaction sessions lost
the automatic-capture signal.

Two port tasks added with detail files — Codex CLI (partial;
missing lifecycle events) and opencode (skills drop in, hooks
need TS rewrite).

### Session B — deferred-work capture + backlog rename (v2.7.2)

Commits (20:56–21:13 local):

- `cb5a9d0` sharpen deferred-work capture + rename tasks.org
- `73a45b3` "the rpm backlog" → "your rpm backlog" in assistant-facing text
- `6f42548` Phase 3a sweep + clear native tasks
- `1c80e8d` Phase 3a auto-promote uncleared natives (no ask)
- `3b9845c` Phase 3a dedup against backlog before promoting
- `330fbf8` bump plugin.json + status.md to v2.7.2

Sharpened "suggest deferrable work → ask" rule with concrete
trigger phrases ("we could X later", "worth Y eventually",
"follow up on Z"). Promoted into SessionStart + resume +
post-compact instruction blocks — same carry-forward pattern as
"Key finding:".

Assistant-facing text renamed: `tasks.org` → "your rpm backlog".
Path literals still read `docs/rpm/future/tasks.org`.

**Phase 3a — Clear native tasks:** at session-end, `TaskList`
sweep collects in-progress/pending natives. Each is scored against
the live backlog (same 100/80/60/40 buckets as `task-capture.sh`).
- ≥80 match: flip matching TODO → IN-PROGRESS (work was started);
  do NOT append — that's the duplicate case.
- <80 match: append as a new TODO at the bottom of a sensible
  parent.
Then `TaskUpdate` on every surfaced task to clear the live list.
Creation-time was the vetting step — no user question.

## Accomplished
- Plugin v2.7.1 + v2.7.2 published.
- Native task / backlog surfaces decoupled; Phase 3a clears the
  live list each session.
- "Key finding:" + deferred-work rules uniform across all
  session-injection paths.
- Codex CLI + opencode port tasks captured in backlog.

## Key Discoveries
- "Tell Claude not to do X" must be repeated on resume /
  post-compact, not just startup — session continuations lose the
  directive otherwise. Same pattern as "Key finding:" carry-forward.
- A completed native with no backlog counterpart is ephemeral
  session sub-work and should be let die. But in-progress/pending
  natives should always auto-promote — creation was the vetting
  step. Asking again at session-end adds friction without signal.
- Dedup before promotion (Phase 3a step 2) is load-bearing —
  without it, a native picked from the backlog at session-start
  would re-append on session-end, doubling the entry.

## Next (from a4ee0e19's perspective, unwritten at the time)
- Session `a4ee0e19` exited without `/session-end` twice on
  2026-04-16 (10:29 and 17:33); exit stubs captured in
  `past/2026-04-16.md`.
- Session 2026-04-17 (f81e37d4): `/audit project` + tracker
  refresh — this backfill is part of that follow-through (T2).
