#+TITLE: rpm Future
#+TODO: TODO IN-PROGRESS BLOCKED | DONE

* Active
** DONE Submit latest to anthropics/claude-plugins-official marketplace
   CLOSED: [2026-04-17]
   :PROPERTIES:
   :ID: marketplace-submit
   :END:
   - Submit via claude.ai/settings/plugins/submit
   - Latest: SHA 59a47b5 (tag v2.5.4) — supersedes v2.5.3
   - Prefer unpinned source if the form allows, so future version
     bumps auto-propagate to users; otherwise expect to re-submit
     on each release

** DONE Tighten session-end native-task reconciliation
   CLOSED: [2026-04-14]
   :PROPERTIES:
   :ID: tighten-native-reconciliation
   :END:
   - Session-end Phase 1 prep auto-append removed; in-progress/pending
     natives now surface in Phase 3a for user-confirmed promotion,
     then get mass-`TaskUpdate`'d to `completed` to clear the live
     list. Completed-but-unmatched natives let die as ephemeral.

** TODO Port rpm to Codex CLI [[file:2026-04-14-codex-port.md]]
   :PROPERTIES:
   :ID: codex-port
   :END:
   - Skills port 1:1 (same SKILL.md + YAML frontmatter format)
   - Plugin manifest similar (.codex-plugin/plugin.json)
   - Hooks are experimental — only SessionStart, Stop, PostToolUse (Bash only);
     no SessionEnd, PreCompact, PostCompact, TaskCreated/TaskCompleted
   - Agents need translation: TOML not markdown
   - No native task UI (no TaskCreate/TaskList/TaskUpdate equivalent)
   - Overall: about half ports; rest blocked on missing lifecycle events

** TODO Port rpm to opencode [[file:2026-04-14-opencode-port.md]]
   :PROPERTIES:
   :ID: opencode-port
   :END:
   - Prototype validated end-to-end (2026-04-18): event→hook bridge,
     skills mirror, agent translation. Remaining work is packaging,
     plumbing, and validation — see sub-tasks.
   - Source of truth: `opencode/.opencode/plugins/rpm.ts`,
     `scripts/sync-opencode.sh`, `scripts/translate-agent.py`.

*** TODO opencode port: distribution (npm vs subtree-split)
    :PROPERTIES:
    :ID: opencode-port-distribution
    :END:
    - Decide whether `rpm-opencode` ships as an npm package or a
      `git subtree split --prefix=opencode` branch (mirroring how
      `plugin/` publishes to GitHub).
    - npm matches opencode's documented install path
      (`opencode plugin install rpm-opencode`); subtree-split keeps the
      git-native workflow we already use for the CC plugin.

*** TODO opencode port: bundle plugin/hooks into the plugin package
    :PROPERTIES:
    :ID: opencode-port-bundle-hooks
    :END:
    - Prototype resolves hooks at `${plugin-file-dir}/../../../plugin/hooks`
      — works only inside the monorepo.
    - For standalone install, `plugin/hooks/*.sh` must be copied into
      the plugin package at publish time and resolved via
      `${import.meta.dir}/hooks/<name>.sh`.
    - Extend `scripts/sync-opencode.sh` to also populate
      `opencode/.opencode/plugins/hooks/`, or handle it in a publish
      script.

*** TODO opencode port: verify skill bash blocks run under opencode
    :PROPERTIES:
    :ID: opencode-port-skill-shell
    :END:
    - rpm skills use `${CLAUDE_SKILL_DIR}`, `${CLAUDE_PLUGIN_ROOT}`,
      and `!bash` inline shell blocks (see init-rpm, session-end, audit).
    - Confirm opencode honors the same env vars + `!bash` sigil, or
      implement an equivalent via `$` in the TS wrapper / a shim.

*** TODO opencode port: slash-command surface
    :PROPERTIES:
    :ID: opencode-port-slash-commands
    :END:
    - Claude Code auto-exposes `/skill-name`; opencode has a separate
      slash-command spec. Verify rpm's six skills are invocable via
      `/<name>` in opencode, or mirror into opencode's command format
      if not.

*** TODO opencode port: test session.deleted against real exits
    :PROPERTIES:
    :ID: opencode-port-session-deleted-exits
    :END:
    - Only verified via explicit `DELETE /session/<id>`. Test `/exit`,
      terminal close, SIGKILL, crash.
    - If `session.deleted` doesn't fire on these, SessionEnd coverage
      regresses — need an alternative (e.g. `session.idle` with a
      debounce, or opencode-side crash handler).

*** TODO opencode port: test first-session workaround in real TUI
    :PROPERTIES:
    :ID: opencode-port-first-session-tui
    :END:
    - Inline-init workaround (SessionStart logic runs inside plugin
      init, not via `session.created`) proven only against
      `opencode serve` + POST.
    - Run an actual interactive `opencode` TUI session and confirm the
      marker writes correctly and context is injected.

*** TODO opencode port: test suite for the TS plugin
    :PROPERTIES:
    :ID: opencode-port-tests
    :END:
    - bats covers Claude Code bash hooks; nothing covers the TS plugin.
    - Write a Bun/node test that loads `rpm.ts` against a mocked
      PluginInput, fires synthetic `session.created`/`session.deleted`
      events, and asserts the correct bash hooks are invoked with the
      right env + stdin.

** TODO Watch SessionStart:clear flake on Opus 4.7 [[file:2026-04-17-sessionstart-clear-flake.md]]
   :PROPERTIES:
   :ID: sessionstart-clear-flake
   :END:
   - User hit a case where /clear on Opus 4.7 did not fire the hook;
     quit+restart recovered it.
   - Harness-level (CC) bug, not rpm code. Watch for repro and file
     upstream at anthropics/claude-code if it persists.

** TODO scan.sh version=unknown fallback [[file:2026-04-18-scan-version-fallback.md]]
   :PROPERTIES:
   :ID: scan-version-fallback
   :END:
   - `session-end` scan.sh emits `version=unknown` because
     `$CLAUDE_PLUGIN_ROOT` isn't set in skill-body bash context.
   - Fix: fall back to `$CLAUDE_SKILL_DIR/../../.claude-plugin/plugin.json`.
   - Ship as v2.7.4; ~10 min including a bats case.

** DONE Extend bats coverage to remaining hooks
   CLOSED: [2026-04-13]
   :PROPERTIES:
   :ID: bats-extend
   :END:
   - 6 new bats files: context-monitor, pre-compact, post-compact,
     session-end-hook, stop-learn-capture, task-capture.
   - Suite: 26 → 69 passing tests.

** DONE Soften session-end nudges (v2.5.4)
   CLOSED: [2026-04-13]
   :PROPERTIES:
   :ID: soften-session-end-254
   :END:
   - Hooks: context-monitor, session-start-auto, session-end,
     handoff-validator. Skill: session-end SKILL.md.
   - Plugin v2.5.4 published — origin/master at 59a47b5, tag v2.5.4.
