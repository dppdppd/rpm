#+TITLE: rpm plugin Tasks
#+TODO: TODO IN-PROGRESS BLOCKED | DONE
#+PROPERTY_FORMAT: Optional :PROPERTIES: drawer with :ID: and :BLOCKED_BY:
#+EXAMPLE:
#+  ** TODO Some task
#+     :PROPERTIES:
#+     :ID: some-task
#+     :BLOCKED_BY: other-task
#+     :END:

* Post-init backlog
** DONE Sync command-version/ to current plugin command surface [2026-04-08]
   - Updated dispatcher pm.md, replaced audit-normal/audit-heavy with audit.md, added session-start/session-end, replaced research.md with deep-research.md
** DONE Run /pm:audit (light) to baseline doc health after init [2026-04-08]
** DONE Add a sample /pm:session-start dry-run note to README so new users see what to expect [2026-04-08]
   - Shipped in c2475ce as a new "What a session looks like" README section.
** TODO On publish: add homepage + repository fields to .claude-plugin/plugin.json, finalize marketplace install instructions in README
** TODO Investigate ~pm-session-active disappearance at /exit→resume boundary
   - One-time quirk observed this session. Marker persisted fine during normal session activity, but vanished between /exit and resume. Root cause unknown — possibly a cleanup process treating ~-prefixed files as backups, or installed hook still using old docs/pm/tmp/ path. Revisit if recurs.
** DONE /pm:audit project skips the mechanical VALIDITY/COHERENCE scan [2026-04-08]
   - Fixed by launching pm:auditor in background at start of Project Phase 1 (plugin version) and inlining the Documents scan spec into Project Phase 1 (dispatcher version). Mechanical findings are explicitly called out as Phase 4 evidence inputs. Plugin → 1.0.17.
** DONE Second /pm:audit project run on plugin's own repo [2026-04-09]
   - 8 findings, plan + report saved to reviews/2026-04-09*. First project-target audit after the 1.0.17 scan-gap fix — validated background pm:auditor scan feeding Phase 4 as designed. User picked option C (commit to phased skills migration) for S1.
** DONE Execute mechanical audit findings A1-A5 + S3 [2026-04-09]
   - A1: PM.md version-sync self-contradiction fixed
   - A2: PM-LOG.md backfilled with 2 missing sessions (done during audit run)
   - A3: commands/init.md stale "pick a depth from the menu" text replaced (+ mirror)
   - A4: CLAUDE.md one-line read-first guardrail added
   - A5: agents/auditor.md off-schema color/whenToUse fields removed
   - S3: ccpm disambiguation note added to pm.md + mirror + skill
** DONE Skills-first migration phases 1-5 [2026-04-09]
   - Phase 1: /pm:pm → skills/pm/SKILL.md (1.0.18, a7df55b)
   - Phase 2: /pm:init → skills/init/SKILL.md disable-model-invocation (1.0.19, 9cf1438)
   - Phase 3: /pm:audit decomposed into SKILL.md + findings-menu.md + project-mode.md (1.0.20, f8b0b1b)
   - Phase 4: session trio — session-start, session-update default-allow; session-end with pre-flight gate for proactive long-context recommendation (1.0.21, 61eca6b)
   - Phase 5: command-version/ frozen, no more mirror maintenance, CLAUDE.md Layout/Editing rewritten (1.0.22, 3b37c88)
   - Legacy commands/*.md preserved as rollback target
** DONE Skills migration phase 6 — remove legacy commands/ [2026-04-09] (2.0.0)
   - Deleted all 6 commands/*.md files. CLAUDE.md Layout swept for stale `commands/` references. PM.md Project Summary + Key Files + Focus Areas updated to describe the skills-first state. command-version/ dispatcher untouched (separately frozen per Phase 5). Verified via scan.sh — 0 broken refs after edits. Dogfood cycle the phase was gated on = the 2026-04-09 session that shipped it. Commit `d85c66a`.
** DONE Bundle deterministic bash scripts for session-end drift scan [2026-04-09] (1.0.23)
   - Shipped as `skills/session-end/scripts/scan.sh`. Six sections in key=value format: git / claude_md / not_implemented / broken_refs / daily_log / session_marker. Auto-injected via `!${CLAUDE_SKILL_DIR}/scripts/scan.sh` so output is already in context when Claude starts Phase 1 — zero tool calls for the scan. Caught 45 false-positive broken_refs during development; tightened to zero via `/`-required + reject-CamelCase + exclude append-only docs heuristics. Phase 1 + 2 restructured for parallel reads/writes; project-mode audit gained "repetitive LLM work → scripts" dimension. Commit `06c7d5d`.
** DONE Extend scan.sh with inventory/staleness checks + audit quick mode [2026-04-09] (2.1.0)
   - Added two sections to scan.sh: `specs_inventory` (checks specs/, spec/, docs/specs/, docs/spec/ against PRESENT.md mentions) and `pm_docs_staleness` (log/tracker/inventory files under docs/ and docs/pm/ with days-since-last-commit). Added `/pm:audit quick` target — runs scan.sh only, no pm:auditor subagent, for fast on-demand drift checks between session-ends. Motivated by volta 2026-04-09 audit findings that scan.sh previously missed (55 unlisted specs + stale parity-fix-log). Dogfooded against volta: total=107 listed=106 unlisted=1, caught 2 loose log files. Commit `7242f34`.
** DONE Session-start PRESENT.md drift check [2026-04-09] (2.1.1)
   - Added a one-liner git-log check to skills/session-start/SKILL.md Phase 2: `git log LAST..HEAD` where LAST is PRESENT.md's last-modified commit. Surfaces any commits landed since PRESENT.md was last updated and asks the user to reconcile before picking a task. Motivated by this session's own experience — started with PRESENT.md three versions behind reality (1.0.22 vs 2.1.0). Closes the "commits outside session-end don't trigger PRESENT.md update" drift-slip loop. Self-healing: the next time the same drift happens, it gets caught at session-start automatically. Commit `222eb03`.
** DONE Investigate session-end auto-inject scan.sh permission prompt [2026-04-09] (2.1.2)
   - Root cause: `!${CLAUDE_SKILL_DIR}/scripts/scan.sh` expands to an absolute path (`/home/.../scan.sh`) that matches no `Bash(...)` allow rule in allowed-tools or settings.local.json. The project already permits `Bash(bash:*)`, so wrapping the auto-inject in `bash "..."` (same shape `hooks/hooks.json` uses) gets it matched. Applied in `skills/session-end/SKILL.md` line 56 (+ `Bash(bash:*)` added to allowed-tools) and `skills/audit/SKILL.md` line 64 (the `/pm:audit quick` cross-skill auto-inject; bare `Bash` in frontmatter so no update needed). Secondary: `session-start` drift-check pipeline `LAST=$(git log …) && git log …` starts with `LAST=`, not `git log`, so it didn't match `Bash(git log:*)`. Split into two sequential bash calls, each starting with `git log`. Dogfood-verification deferred to next `/pm:session-end` invocation. Commit `029c62b`.
** DONE Session-start scan.sh auto-inject for faster startup [2026-04-10] (2.1.3)
   - Created `skills/session-start/scripts/scan.sh` bundling latest past-file lookup, git status/stash, and PRESENT.md drift check. Auto-injects before skill body loads. Rewrote SKILL.md to consume scan output. Cuts ~5 sequential tool-call rounds to ~2. Commit `c17dbcf`.
** DONE Session-end latency optimizations [2026-04-10]
   - Pre-read today's past log in Phase 1b, merge commit+presentation, merge rm+handoff. ~4 rounds instead of ~6. Commit `f605ead`.
** DONE Session-end UX improvements [2026-04-10]
   - No empty menu items, questions at end only, handoff instructions last. Commit `b50423a`.

* Competitive-gap features [2026-04-10]
** DONE Compaction guard — PreCompact/PostCompact hooks to preserve session state [2026-04-10] (2.2.0)
   :PROPERTIES:
   :ID: compact-guard
   :END:
   - PreCompact hook saves snapshot + checkpoints to daily log; PostCompact re-injects recovery state. Also handles mid-session checkpointing (replaced /pm:session-update). Commit `c53f699`.
** DONE Auto learning capture on Stop hook [2026-04-10] (2.2.0)
   :PROPERTIES:
   :ID: learn-capture
   :END:
   - Stop hook greps for learning signals in assistant responses, appends to ~pm-learnings.jsonl. Session-end scan.sh reads and presents excerpts. Commit `c53f699`.
** DONE Structured task state with dependencies in FUTURE.org [2026-04-10] (2.2.0)
   :PROPERTIES:
   :ID: task-deps
   :END:
   - :ID:/:BLOCKED_BY: property drawers with scan.sh validation in both session-start and session-end. Detects dangling refs, reports ready tasks. Commit `c53f699`.
** DONE Automate session-start via SessionStart hook [2026-04-10] (2.2.0)
   - SessionStart hook auto-injects PM context (git state, open tasks, daily log, drift, task deps). Replaced /pm:session-start skill entirely. Commit `c53f699`.
** DONE Remove /pm:session-update — replaced by PreCompact checkpoint [2026-04-10] (2.2.0)
   - Compaction is the right trigger for mid-session checkpoints. Skill deleted. Commit `c53f699`.
** DONE Rename plugin from pm to rpm (Relentless Product Manager) [2026-04-10]
   - All command prefixes, skill directory, agent namespace renamed. docs/pm/ kept. Commit `d443ba6`.
** TODO Dogfood rpm hooks in a fresh session
   - Verify: auto-start hook loads context, Stop hook captures learnings, PreCompact checkpoints, session-end consumes learnings + task deps.
