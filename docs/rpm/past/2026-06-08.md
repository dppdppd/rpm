# 2026-06-08

## Accomplished
- Built rpm's own **collapse-proof research Workflow** (`plugin/skills/research/rpm-research.workflow.js`, Claude-Code-only): Scope → Search → Fetch → INDEPENDENT per-lens verification panel → Synthesize. Wired the `research` SKILL.md to it via a CC fast-path section (self-gates to prose on opencode/codex); sync scripts exclude the `.workflow.js` from both ports. Proven end-to-end on a smoke run.
- Ran it as the **4th column of the VOC Dutch-primary bake-off**, graded blind: near-native fidelity (Q1/Q2a/Q3 CORRECT), **fully fixes the old-v2 "Captain Vogel" fabrication**, kill-and-replace positively delivers the right answers — at ~5.4M tokens (2.3× native).
- **Two tuning passes:** round-1 (agent-budgeted verify cap 120→48; `flag`→`contested` surface-both) flipped Sub-Q2b PARTIAL→correct (fidelity 4→0) at ~3.87M tokens; round-2 tightened `contested` to corroborated flags only, guaranteed per-dimension fetch coverage, and added a multi-MB-artifact cost guard.
- 6 commits (`f01dddb`,`6d2be0c`,`8fed8b6`,`fcb3447`,`3b2e2fe`,`7d53552`); bats 212/212 throughout; backlog `rpm-research-workflow-{tuning,round2}` DONE.

## Key Discoveries
- The bake-off's v2 "regression" was **fan-out collapse** (run nested as a subagent → no `Agent` tool → the panel self-certifies), not broken lens prose. A Workflow makes the panel structurally independent.
- **kill-and-replace delivers, not just refuses** — adopting a better-sourced rival is what lifts the arm from old-v2 to near-native; merely killing leaves a hole.
- **Collapse-proof ≠ cheap.** The independence that prevents self-certification is exactly what makes the panel expensive (120 verifiers > native's whole run). The cap cut agents 120→48 but tokens only ~28% (muddied by a 2.79 MB PDF).
- **The same mechanism that fixes under-delivery can cause over-delivery:** `flag`→`contested` flipped Sub-Q2b correctly but over-hedged 3/8 claims until round-2 required corroboration.

## What Didn't Work
- First smoke test was orphaned mid-Verify when the prior session ran out of context — not a script bug (diagnosed from the run journal: 26 agents, last verifier cut off mid-Bash-call).
- Cost parity with native NOT reached (still ~1.67× after round-1); a re-measure is deferred to a future live run (token budget).
- Sub-Q3 regressed in the tuned run (Scope made 4 dims, Fetch grabbed no van Riebeeck source) — Phase 1–3 variance, addressed structurally in round-2.

## Next
- (parked, budget-gated) Live re-run the round-2 workflow on the VOC probe to confirm cost drops toward native and Sub-Q3 fetch coverage holds.
- Backlog top: `injection-weak-model-arm` (needs a non-Claude runtime).
