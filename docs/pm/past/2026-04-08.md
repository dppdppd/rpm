# 2026-04-08

## Accomplished

### `/pm:audit heavy` — first full consultant review of the plugin

- Ran the first Heavy audit on the plugin's own repo. Phase 1
  investigated the full plugin surface (`commands/`, `agents/`,
  `skills/`, manifests, `command-version/` mirror, hooks, all
  `docs/pm/*`). Phase 2 validated against `code.claude.com/docs`.
- **Phase 3 (outward / competitive research) was added mid-run**
  after the user flagged that the original Phase 2 was purely
  inward. That redirection became a meta-finding against the
  audit command itself.
- Outcome: 5 inward findings + 3 outward findings = **8 findings,
  all approved and executed in-session**.

### Executed all 8 audit findings

1. **README install instructions** rewritten — dev-mode,
   persistent-local marketplace-add, and "when published" blocks.
   Repo is unpublished, so the marketplace handle is a placeholder.
2. **`/pm:audit` Standard Phase 1 rewired** to invoke the
   `pm:auditor` subagent by name. Detailed scan spec promoted into
   `agents/auditor.md` as single source of truth. Inline prompt
   removed from the plugin version; dispatcher version keeps the
   inline prompt (no subagent mechanism there — intentional
   divergence, same pattern as `deep-research`).
3. **Root `plugin.json` deleted.** `CLAUDE.md` simplified to
   reference only `.claude-plugin/plugin.json` as the canonical
   manifest.
4. **`LICENSE` created** (MIT, © 2026 dppdppd).
   `.claude-plugin/plugin.json` gained `license` and `keywords`.
   `homepage` / `repository` deferred to publish-time (FUTURE.org
   TODO added).
5. **`deep-research` output** redirected from project-root
   `research/` to `docs/pm/research/` in both the plugin skill and
   the dispatcher variant.
6. **`/pm:session-update` command created** — lightweight
   mid-session checkpoint. Appends `### HH:MM update` subsection to
   `docs/pm/past/YYYY-MM-DD.md` and refreshes `PRESENT.md`. No
   menu, no commit, no `FUTURE.org` touch. Competitive gap closed
   vs `claude-sessions` (iannuttall). Propagated through every
   command table (`commands/pm.md`, `command-version/pm.md`,
   `README.md`, `CLAUDE.md`, `docs/pm/PM.md`).
7. **Native `TaskCreate/TaskUpdate/TaskList` integration** with
   `FUTURE.org` as the source of truth. `session-start` now
   hydrates a native task for the picked FUTURE.org item (with
   `activeForm` for the spinner); `session-end` Phase 2 reconciles
   `TaskList` with `FUTURE.org` (mark DONE, append new, never
   delete).
8. **`/pm:audit` Heavy mode restructured** into five phases:
   Investigate → Inward Research → Outward Research → Analyze →
   Refine. Outward research is marked **REQUIRED**, not optional.
   Added `Competitive Gaps` as the 7th analyze dimension. Plan-file
   template gained `Competitive` as a valid Dimension. Mirrored to
   `command-version/pm-commands/audit.md`.

### Plugin version bumped to 1.0.13

## Key discoveries

- **Claude Code plugin schema puts the manifest in
  `.claude-plugin/plugin.json` exclusively.** A root-level mirror
  is not part of the spec — it was pure maintenance burden.
- **`agents/auditor.md` was dead code** — registered as
  `pm:auditor` but never invoked by its own command. The inline
  prompt in `/pm:audit` Standard Phase 1 had already diverged from
  the agent's spec (auditor had 8 short bullets; audit.md had 8
  detailed bullets).
- **Heavy audit's biggest failure mode is over-indexing on inward
  research.** Caught only because the user flagged it manually.
  Command wording now makes outward research required and distinct.
- **Claude Code ships native persistent Tasks** (`~/.claude/tasks/`)
  since Jan 2025 with deps, blockers, and `activeForm` spinner. We
  were duplicating a worse version in `FUTURE.org` alone. Hybrid
  integration done.
- **Competitive landscape snapshot** (from Phase 3 outward research)
  saved in `docs/pm/reviews/2026-04-08.md`: `claude-sessions`,
  Superpowers, Cline Memory Bank, Docs Guardian, native Tasks, Spec
  Kit, Kiro.

## What didn't work

- The first Phase 2 (original Heavy mode wording: "Research") was
  purely inward — validated against Anthropic docs but never
  looked at real alternatives. The user had to redirect the review
  toward competitive analysis. This drove Finding 8 (restructure)
  and is why outward research is now marked REQUIRED.
- One parallel `Edit` to `docs/pm/reviews/2026-04-08-plan.md`
  failed on the first attempt due to an `old_string` mismatch
  (text from a stale mental copy vs the actual file content).
  Retried successfully with the exact on-disk text. Lesson: when
  the same file is hit by two edits in one parallel batch, second
  edit may race against the state I assumed.

## Next

- `/pm:session-end` action menu will pick how to commit the large
  uncommitted set (many files touched across 8 findings).
- Next session candidate: the remaining open `FUTURE.org` TODO
  — "Add a sample `/pm:session-start` dry-run note to README so
  new users see what to expect".

### 17:23 update
- Accomplished since session-start:
  1. README gained a "What a session looks like" dry-run section
     (shipped in `c2475ce`, plugin bumped to 1.0.14). Closes the
     only actionable FUTURE.org TODO.
  2. Session marker relocated from `docs/pm/tmp/pm-session-active`
     → `docs/pm/~pm-session-active`. Removes the `mkdir -p` step
     in `/pm:session-start`. 8 references updated across
     `commands/`, `command-version/pm-commands/`, hook script, and
     README. Version bumped to 1.0.15 (uncommitted).
- In progress: marker-relocation edits staged, not yet committed.
  Pending user decision on audit-task arrangement (new request).
- Discoveries:
  - **The `~pm-session-active` marker disappeared between session
    /exit and resume.** Root cause unknown. Not the install
    plugin's hook (which took the else branch per the reminder
    message). Possible: a cleanup process treating `~`-prefixed
    files as backups, or `/exit`-time cleanup of scratch files.
    Needs investigation before shipping the relocation.
  - Install plugin's hook still points at the old
    `docs/pm/tmp/pm-session-active` path — working-dir edits to
    `hooks/*.sh` don't propagate until the plugin is reinstalled.
- Native tasks: 2 completed · 0 in-progress · 0 pending

### 17:45 update — audit restructure
- Marker relocation committed as `e65ff57` (1.0.15). The
  disappearance was a one-time quirk at /exit→resume; marker has
  persisted through normal session activity. Added `.gitignore`
  entry so the ephemeral file doesn't show up in `git status`.
- **User flagged major weakness:** "it's not clear when to execute
  the various audits." Walked through what `light`/`standard`/
  `heavy` actually do by reading `commands/audit.md` +
  `agents/auditor.md` end-to-end.
  - Discovery: my first guess (`/pm:audit code`) was wrong —
    `standard` doesn't audit code. Its 8 passes cover docs +
    CLAUDE.md effectiveness + memory alignment + session-drift
    mining. Only `heavy` reads source.
  - Discovery: `light` is 100% a subset of `standard`. It exists
    only as the "cheap" version.
  - Discovery: `heavy` skips standard's mechanical VALIDITY/
    COHERENCE scan entirely — a `heavy` run can miss broken refs
    that `standard` would catch.
  - Discovery: `/pm:session-end` currently has NO drift check
    whatsoever.
- **Restructure approved (user picked `documents` over my
  `workflow` suggestion):**
  1. `/pm:audit` now takes one arg: `documents` or `project`.
     No depth menu, no recency recommendation.
  2. `documents` = today's `standard` (pm:auditor subagent,
     scored, hookify).
  3. `project` = today's `heavy` (5-phase consultant review).
  4. `light` dropped. Its cheap checks folded into
     `/pm:session-end` Phase 1e as an inline drift scan:
     broken refs, CLAUDE.md size, NOT_IMPLEMENTED, tracker
     consistency, stale daily-log.
  5. Session-end Phase 3 gains a "Doc-drift scan" report section
     and Phase 4 gains a "Fix drift" action.
- Files edited: `commands/audit.md` (rewritten), `commands/
  session-end.md` (drift scan + action), `commands/pm.md`
  (table + Output Rules), `README.md` (table), `docs/pm/PM.md`
  (summary + key files). Mirrored to `command-version/`.
  Plugin bumped to 1.0.16.

### 18:00 — session end

Drift scan ran for the first time against the plugin's own repo.
Found one real drift finding: `FUTURE.org` still listed the
session-start dry-run note as TODO despite shipping in `c2475ce`.
Phase 2 reconciled it to DONE automatically. Everything else
clean: CLAUDE.md at 53 lines, all backticked refs resolve, no
real `NOT_IMPLEMENTED` stubs (9 matches are all meta-references
inside audit command docs).

**Session delivered three commits, plugin 1.0.13 → 1.0.16:**
- `c2475ce` — README "What a session looks like" dry-run example
- `e65ff57` — session marker → `docs/pm/~pm-session-active`, `.gitignore` added
- `0a93738` — audit restructured into `documents` + `project`, light dropped, drift scan folded into session-end Phase 1e

Net: audit surface dropped from 3 depths (light/standard/heavy)
to 2 targets (documents/project) + automatic hygiene at session-
end. Trigger model finally matches what each mode actually does.

Open item: marker disappearance at /exit→resume remains
unexplained. Added to FUTURE.org.
