# 2026-04-28 — A/B test W&D parallel-tool-calling vs 4-subagent design

## Accomplished
- Ran full `/deep-research` on "how to best utilize LLMs for deep research"
  — 4 parallel sonnet sub-agents, 12 primary sources fetched, gap analysis,
  adversarial validation, dedicated citation-audit agent (~82% verified).
  Output at `research/llm-deep-research-best-practices/findings/report.md`.
- Implemented all 5 recommendations from the report into both
  `plugin/skills/deep-research/SKILL.md` and
  `~/.claude/commands/deep-research.md`:
  - Data-only delimiters for fetched URL content (prompt-injection defense)
  - URL liveness pre-check (urlhealth-style; arXiv:2604.03173)
  - Source-grounded confidence tags (require URL or label "model knowledge — not verified")
  - Reflection-driven Grader check between Round 1 and Round 2
  - Layered citation defenses (deterministic URL liveness → semantic audit → optional inline self-verification)
  Plugin bumped 2.7.11 → 2.8.0.
- Changed `plugin/hooks/context-monitor.sh` hard 50k threshold to 10% of
  context window (dynamic to `RPM_CONTEXT_TOKENS`); updated bats tests
  and README. Now warns at <100k remaining on 1M default and <20k on 200K
  override — proportionate across both window sizes.
- Ran experimental A/B comparing 4-subagent design (Variant A) vs.
  single-agent W&D-style parallel tool calling (Variant B) on a
  3-system × 5-question survey:
  - Variant A: 70,060 tokens, 168s wall-clock, 36 WebSearch calls
  - Variant B: 30,428 tokens (−56.6%), 122s (−27%), 19 calls (−47%)
- Wired SURVEY vs DEEP-DIVE task-shape classifier into Phase 1 of both
  the rpm plugin SKILL and the standalone command. Added new SURVEY
  strategy section in Phase 2 with full agent prompt template; Scaling
  Rules doubled (Deep-dive {Quick/Focused/Deep} + Survey
  {Quick/Focused/Deep}). Plugin bumped 2.8.0 → 2.9.0.

## Key Discoveries
- W&D pattern (parallel tool calls inside one agent) cuts tokens ~57%
  on comparison-shaped tasks vs. multi-subagent, and surfaces
  cross-entity patterns that are architecturally invisible to
  multi-subagent designs (each sub-agent is sandboxed to one entity).
- urlhealth (arxiv:2604.03173) is a deterministic, non-LLM check that
  cuts non-resolving citation URLs by 6–79× to <1% — pure HTTP HEAD,
  no model needed.
- 3–13% of citation URLs in production DR agents are fully fabricated
  (no Wayback record). 5–18% non-resolving overall. DR agents
  hallucinate URLs at higher rates than simpler search-augmented LLMs
  because they generate more citations.
- Indirect prompt injection in fetched URL content is in the wild —
  Comet AI summarizer leaked an OTP after reading hidden Reddit-post
  instructions (Apr 2026). Curl-fetched HTML must be wrapped as
  data-only.
- Verbalized confidence from RLHF models is poorly calibrated:
  GPT-4 AUROC on its own stated confidence ≈ 62.7%; ECE > 0.377
  across GPT-3/3.5/Vicuna (arXiv:2306.13063). Bare H/M/L tags are
  barely better than random — must be source-grounded to be useful.
- None of the major open-source DR agents (LangChain ODR, MiroThinker,
  GPT-Researcher) implement runtime HTTP URL-resolution checks as of
  2026 — universal gap across the ecosystem.

## What Didn't Work
- First curl batch in the original deep-research run leaked 11 of 12
  fetched HTML files to project root: `cd $dir && curl1 & curl2 & ...`
  parses as `(cd && curl1) &` followed by `curl2 &` etc., so only the
  first curl ran inside the cd-affected subshell. Fix: cd in a
  separate command, or wrap the whole batch in `(cd $dir && (curl1 &
  curl2 & ... wait))`.
- Citation-audit agent reported AgentIR conventional-baseline as 52%
  but the fetched abstract said 50%. Resolved by hedging in the
  report ("~50%; audit reported 52%; discrepancy unresolved, doesn't
  change qualitative claim") rather than picking one.

## Continuation (afternoon — Recs 7 & 8, all remaining items, release)

### Accomplished (continued)
- **Rec 7 — inline verify-as-you-write** shipped as v2.10.0 (commit
  c98ee1f). Phase 5b now requires the orchestrator to re-Read the
  relevant `fetched/` artifact window for every load-bearing claim
  *before* writing the paragraph; revise or drop on source mismatch,
  never write from memory when the source disagrees. Phase 5c's
  Layer 3 (optional inline verification) collapsed back into 5b
  since it's now the writing contract.
- **Rec 8 — DRB II eval baseline.** Fetched DRB2 `tasks_and_rubrics.jsonl`
  (132 tasks, github.com/imlrz/DeepResearch-Bench-II), picked task8
  (Materials Inverse Design — Science & Tech, English, ~52 rubrics),
  ran a single SURVEY-mode sub-agent at 29.3k tokens / 181s / 17 tool
  uses, scored against all 52 rubrics: **33/52 strict (63.5%) ·
  40/52 lenient (76.9%)**. Per-dimension: analysis 11/12 (91.7%),
  presentation 3/3 (100%), info_recall 19/37 strict (51.4%) — info
  recall was the dominant gap, driven by database identity drift
  (5 of 7 expected DBs found, 2 missed), URL form mismatch, algorithm
  classification (PSO under Optimization-based when rubric expected
  Exploration-based), and missing specific algorithm names (Topology
  Optimization, Forward/Inverse Model Integration, Neural Networks
  as a standalone). DRB2 paper reference: <50% across full benchmark.
  Scaffolding at `research/drb2-baseline/{tasks,runs,scoring,findings}/`
  ready for additional task runs. Caveat: **n=1, abridged Phase 0–2
  only** (no URL fetch, no Phase 4 gap/adversarial, no Phase 5b inline
  verify, no Phase 5c citation audit).
- **3 DRB2-derived improvements shipped as v2.11.0** (commit ca80c80):
  Phase 2 agent template anchors on user's categorization (don't
  silently reorganize into popular literature convention); Phase 4
  explicit domain-coverage check (region-specific + specialty + niche
  sources, not just globally popular); Phase 5b URL canonicalization
  (canonical landing page, not any-2xx — won't 404 in six months).
- **Anthropic Managed Agents migration scoped: NO**
  (`research/managed-agents-scoping/decision.md`). Four reasons
  documented (wrong runtime target, no durability gap, conflicts with
  local-first artifact model, breaks SKILL.md portability) plus three
  explicit revisit conditions.
- **Gap 1 query-count ablation deferred**
  (`research/query-count-ablation/methodology.md`). Reflection-driven
  Grader check (v2.8.0) makes the budget adaptive per task; the
  fixed-budget question the original gap analysis raised is now
  side-stepped. Methodology stub + cost estimate (~$50–80) + decision
  criteria left for any future run.
- **Phase 3 retry/fallback hardening filed to backlog**
  (`docs/rpm/future/2026-04-28-phase3-retry-hardening.md`,
  ID `phase3-retry-hardening`). Borrows patterns from Managed Agents
  WITHOUT runtime migration — exponential backoff for HTTP 429,
  per-domain failure threshold, batch-wide timeout detection. Defer
  until observed in practice.
- **Released v2.11.0** via `scripts/publish-all.sh`:
  - `dev` (Gitea full repo, york:3333) — `master` → `167bb14`
  - `plugin/master` (CC subtree, GitHub) — force-pushed to `5d2e267`
  - `plugin/opencode` (opencode subtree, GitHub) — force-pushed to `eb54a81`
  - tag `v2.11.0` pushed to plugin remote at `167bb14`
  First tag since `v2.7.10`, so this release covers v2.7.11 through
  v2.11.0 in one cut.

### Key Discoveries (continued)
- **Per-claim verification beats post-hoc audit** for "Frankenstein
  citation" failure mode (real fragments stitched into a fake claim).
  VeriFact-CoT pattern (arXiv:2509.05741): 72→83% factual accuracy
  and 25→12% hallucination when verification moves into the writing
  loop instead of running after. Cost: ~1 small Read per cited claim
  during synthesis (~30 Reads on a typical 30-claim report).
- **DRB2 task8 baseline showed analysis (91.7%) and presentation
  (100%) are not the bottleneck — info_recall (51.4%) is.** The skill
  correctly captures conceptual framing and report structure; what
  it misses is niche/regional sources, exact URL forms, and the
  user's stated taxonomy. All three are addressable via prompt-level
  guardrails (v2.11.0).
- **Generic web search systematically under-weights region-specific
  sources.** DRB2 task8's reference report listed 7 expected
  databases including DDSE (a specialty solid-state-electrolyte DB)
  and ASM Alloy Center; the baseline run found 5 of 7 plus 3
  globally-popular alternatives (AFLOW, Materials Cloud, JARVIS).
  Phase 4 now mandates explicit checking for region-specific +
  specialty entries.
- **W&D pattern stays cheap on simple SURVEY tasks too.** Initial
  experiment (3 systems × 5 questions = 56.6% savings); DRB2 task8
  run (3 method categories + DB list, again survey-shape) used 29.3k
  tokens — confirmed the savings hold on a fresh, real-world task.

## Doc-drift fixes
- Marked `broaden-hookify` (already shipped in commit b6dbb03 / v2.7.11)
  and `archive-done-entries` (already shipped in v2.7.10) as DONE
  during Phase 3b reconciliation; archived to `done.org`.

## Next
- **Top of backlog**: Port rpm to Codex CLI. The deep-research
  thread that drove this session's 5 plugin releases is now fully
  discharged — every recommendation is shipped, scaffolded, or
  formally deferred.
