# rpm Log — 2026-04-18

## Active Tasks
- context-monitor threshold rework + session-directive consolidation

## Accomplished
- Context monitor: replaced two-tier 75%/90% with single "under 50k tokens
  remaining" threshold (commit `a3f3a1c`). Message rewritten — it no longer
  references compaction, since at the 1M limit no auto-compaction happens.
- Added a **QUESTION:** prefix directive — every question to the user now
  starts with the literal label "QUESTION:" for visual unmistakability.
  Saved as feedback memory and codified in the rpm hooks.
- Extracted shared session directives into `plugin/hooks/_directives.sh`
  (commit `8fa6695`), following the `_scoring.sh` helper pattern from T7.
  Sourced by `session-start-auto.sh` (resume + normal paths) and
  `post-compact.sh`; the 3-line echo block that was duplicated across 3
  hook sites is now a single `emit_rpm_directives` call per site.
- Dropped `## Tasks` and `## Questions` sections from
  `docs/rpm/context.md` and `plugin/skills/bootstrap/SKILL.md` — the
  helper is now the single source of truth.
- Test suite stayed green through both refactors (110 tests).

## Key Discoveries
- **No auto-compaction at the 1M context limit.** The session just hits
  the limit; detail isn't "lost to compaction" the way the hook's prior
  wording suggested. Warning message corrected to "before the session
  hits the limit" instead.
- Consolidation pattern validated: when hooks duplicate the same echo
  block, extract to a `_<name>.sh` helper and source it — matches the
  T7 `_scoring.sh` precedent.

## What Didn't Work
- First pass at the warning message ("detail starts getting lost to
  compaction beyond this point") was inaccurate. User flagged it;
  rewritten mid-session.

## Next
- Pick from the rpm backlog — top actionable is still
  **Draft Reddit post for r/ClaudeAI**.

### 09:59 session ended without wrap-up
- **Session:** 3b38f169-256e-4115-9ac6-4eb3dee267b0
- **Task:** (unassigned)
- **Reason:** prompt_input_exit
- **Uncommitted files:** 0
- **Action:** next session should backfill past/ and commit outstanding work
