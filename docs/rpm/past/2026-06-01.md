# 2026-06-01 — rpm

## Accomplished
- **Renamed the `deep-research` skill → `research`; released v2.21.0.** Hard rename
  (no alias; trigger description intact so "research X" still auto-fires) to break the
  name collision with Claude Code's native bundled `/deep-research` Workflow and
  reposition rpm's skill on its real edge — portability / durable artifacts / inline /
  cost-governance, explicitly not rigor. Touched: `skills/deep-research/` →
  `skills/research/` + `SKILL.md` identity refs (preserving every *native* `/deep-research`
  mention), `research.bats`, `/rpm` command list, `audit/project-mode`, `scan.sh`
  `RPM_SKILLS`, `auditor.md`, `tips.txt`, `CLAUDE.md`, 5 memory files. Both ports
  regenerated from `plugin/`. bats 206/206. Commits `14b1ada` (bake-off plan) +
  `90cec5a` (rename + bump). Published via `publish-all.sh`: dev + CC `master` (`c934728`)
  + opencode (`c46c2af`) + codex (`db15be6`) + tag `v2.21.0`.
- **Assessed rpm:research vs native /deep-research competitiveness.** Concluded the
  question is effectively settled: the team already decided NOT to compete on rigor
  (bake-off found native matches/edges rpm; the audit fan-out spike was rejected at 9.3×
  cost), the real execution gap (citation discipline) is closed and Tier-2-validated, and
  the differentiation axes (portability/artifacts/inline/cost) are architectural. Only
  loose end: the v1→v2 hardening was validated by offline Tier-2 replay, never by a fresh
  end-to-end head-to-head — inferred, not measured.
- **Parked the rigorous v1/v2/native Dutch-primary bake-off** as the top backlog TODO
  (`docs/rpm/future/2026-05-31-dr-v1-v2-native-bakeoff.md`). Surveyed the 28-tree VOC
  corpus and selected `voc-expedition-goals-chamber-assignments-1602-1700` — the most
  Dutch-primary-dense tree (Nationaal Archief 1.04.02 invnrs, Generale Missiven, GLOBALISE).

## Key Discoveries
- The original bake-off specimen (`voc-decline-era`) sits near the BOTTOM of the corpus
  for Dutch-source density (51 tokens) — the first bake-off never stressed Dutch
  primary-source translation. `voc-expedition-goals-…` leads at 1,734 Dutch tokens / 17 of
  21 files. Caveat: even there the report's HIGH-confidence claims lean on English
  secondary describing the archives, so a true translation stress needs added
  Dutch-primary-only sub-questions.
- Both rpm ports are fully GENERATED from `plugin/`: `sync-codex` prunes stale skill dirs
  but preserves `codex-sync: manual` sentinel files; `opencode/.opencode` mirrors are
  gitignored and regenerated at publish. So a skill rename = edit `plugin/` once + re-sync.
- `translate-skill-codex.py` deliberately does NOT swap tool names (WebSearch→web_search) —
  documented design (false-positive risk in prose); every codex skill ships Claude tool
  names and relies on the model to map them.

## Decisions / What Didn't Work
- Dropped the codex `deep-research` manual override rather than re-pinning it under the new
  name. It had drifted stale (205 vs 372 lines); the auto-translate gives current content
  plus the project-standard tool-name handling. Flagged for user veto — a hand-tuned codex
  copy would be a separate follow-up if wanted.

## Next
- v1/v2/native Dutch-primary bake-off (parked, top of backlog) — the ~3.5–4M-token
  "spend real tokens" live run, awaiting go.
