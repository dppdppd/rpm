# 2026-04-14

*Backfilled 2026-04-17 from git log + status.md bullets. Session
`a4ee0e19` continued through this day.*

### Session — task/backlog overhaul (v2.7.0)

Commits (10:29–21:48 local):

- `8a9d610` session-end Phase 5 reconciles tasks.org priority
- `7dace88` trim session-end SKILL (−47 lines)
- `3e18483` TaskCompleted hook scores candidates; CANCELLED as terminal
- `8c11b7b` session-end → 4 user-visible phases with printed headers
- `4af33f5` `/tasks postpone` (defer to bottom of parent group)
- `fcf0ed2` rename phases with more descriptive verbs
- `954fc43` phase header format → `Phase N (of 4): Title`
- `fc0aa4f` bump plugin.json + status.md to v2.7.0

Net: the `/session-end` skill is a printed 4-phase pipeline
(Collecting Findings → Housekeeping → Reviewing Tasks →
Handing Off). Priority reconciliation moves from ad-hoc to
**Phase 3b** with 4 mismatch signals (user worked below the top,
top is blocked, user flagged, user deferred mid-session).

`TaskCompleted` hook writes `~rpm-task-candidates.jsonl` — each
completed native scored against every actionable heading at
completion time (100 exact / 80 substring / 60 Jaccard≥0.6 /
40 Jaccard≥0.3). Session-end auto-applies DONE at ≥80, asks at
40–79.

`/tasks postpone <task>` moves the task to the bottom of its
`* Parent` group, stamps `:POSTPONED: YYYY-MM-DD`. Status stays
TODO.

`CANCELLED` joins DONE as a terminal workflow state — recognized
by the parser and scan.

## Accomplished
- Plugin v2.7.0 published.
- `/session-end` restructured to four named phases with printed
  headers.
- TaskCompleted scoring pipeline + session-end auto-apply.
- `/tasks postpone` procedure.
- CANCELLED as a terminal workflow state.

## Key Discoveries
- Scoring at completion time (while the subject is fresh) beats
  re-deriving from cold conversation context at session-end.
- Naming the phases — Collecting Findings, Housekeeping, Reviewing
  Tasks, Handing Off — plus printing headers gives the user a clear
  progress model. Phase numbers alone were opaque.
