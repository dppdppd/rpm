# rpm — Present State

## Project Status
- **Current phase**: Active development
- **Last updated**: 2026-05-31
- **Version**: 2.20.0

## Completed Work
- Plugin architecture (skills, hooks, agents)
- Session lifecycle (start, compact, end)
- Session-start task menu (hierarchical, interactive)
- Session continuity across /clear and resume (source field detection)
- Audit system (quick, documents, project) with 8 analysis dimensions
- Deep research skill
- /tasks skill for mid-session backlog management
- Learning capture pipeline (Stop hook + "Key finding:" contract)
- PostCompact compact_summary for better continuity after compaction
- Random tips at session start (stderr, not model context)
- /rpm ? quick-reference command
- Context-aware session-end suggestion (replaces time-based nudge)
- Post-clear continuity: next field in ~rpm-last-session, branched startup flow
- File rename migration (FUTURE.org/PRESENT.md/RPM.md/RPM-LOG.md to tasks.org/status.md/context.md/log.md)
- Bootstrap with single detect.sh script, permissions-first flow
- Context.md auto-injection at session start
- SessionEnd hook — warns + stubs daily log when session ends without /session-end
- PostToolUse context monitor — 40/60/70% transcript-size thresholds for wrap-up pressure
- Stop handoff validator — checks /session-end output completeness (Accomplished/Next sections, status.md date)
- TaskCreated/TaskCompleted capture — native task lifecycle persisted to ~rpm-native-tasks.jsonl
- Always-on resume prompt + handoff marker checks (v2.5.1)
- bats test suite (`plugin/tests/`, 111 tests — full hook + scan.sh + score-natives coverage) + GitHub Actions CI (bats + shellcheck)
- Session-start empty-backlog brainstorm flow — when no actionable tasks, review tasks.org for miscategorized items, otherwise draft 2–4 candidates
- Session-start stale detection via session_id mismatch (v2.5.2) — works across `--continue` / new-process flows, not just `SOURCE=startup`
- Proactive session marker written on every fresh session — any work survives the next session's stale check even if the user skips the task menu
- `/session-end` Phase 1e — auto-derive a task title when the marker says `(unassigned)`; no prompt, no "(unassigned)" leaking into daily log / last-session
- Paired session markers (v2.5.3) — `~rpm-session-start` (SessionStart) + `~rpm-session-end` (/session-end) pair on session_id for deterministic orphan detection; softer "wasn't ended with /session-end" stale UX, no task menu on stale path
- v2.5.3 published — plugin-only split at `7f21ead`, tagged `v2.5.3`
- Softened session-end nudges (v2.5.4) — context-monitor drops "HARD WRAP-UP GATE" / "do not start new tasks" language; session-end hook stops printing stderr warnings on unclean exit; session-start stale path becomes a soft note that falls through to the task menu instead of blocking; handoff-validator drops the "review trackers" directive; session-end SKILL description no longer pushes proactive auto-invocation
- Context monitor reads real tokens (v2.5.5) — pulls `input + cache_read + cache_creation` from the latest assistant `usage` block instead of transcript byte size; defaults to a 1M-token window with `RPM_CONTEXT_TOKENS` env override for 200K users; bats coverage extended to override path + transcripts without usage
- Context monitor thresholds raised to 75% / 90% (v2.5.6) — drops the early 40% heads-up and 60% mid-tier; lower thresholds were noisy on the 1M window and triggered session-end pressure long before context loss was real
- Context monitor filters sidechain entries (v2.5.7) — `jq first()` with `isSidechain != true` picks the most recent main-chain assistant usage; subagent/Task runs no longer mask the parent session's true context size
- Single-option menu forms (v2.5.8) — audit findings, /session-end Actions, and /session-end Record findings now switch to a direct `yes / no` prompt when only one option remains, instead of the awkward `1 · all · none` grammar
- marketplace.json source → object form (v2.5.9) — Claude Code marketplace spec requires `{ source: "url", url: "..." }`; a bare URL string was breaking `/plugin marketplace add dppdppd/rpm`
- Session-lifecycle hooks optimized (v2.6.0) — task_menu parser rewritten with bash builtins (~4.6× faster, 900ms→200ms on a 30-task backlog); `session-end.sh` daily-log stub bug fixed (`0\n0` corruption); `scan.sh` caches `git status`; "first response" boilerplate compressed in 4 hooks; `/effort` stderr nag removed
- Task/backlog overhaul (v2.7.0) — session-end restructured into four user-visible phases with printed headers (`Phase N (of 4): Title`): Collecting Findings → Housekeeping → Reviewing Tasks → Handing Off. New behaviors: `TaskCompleted` hook scores completions against `tasks.org` headings (auto-apply DONE ≥80 confidence, ask 40–79); `CANCELLED` workflow state recognized as terminal by parser + scan; Phase 3 reconciles `tasks.org` priority against session reality with 4 mismatch signals; `/tasks postpone` defers a task to the bottom of its `* Parent` group with `:POSTPONED:` stamp; `/tasks review` gets Deferrals dimension. SKILL.md trimmed 47 lines, then re-expanded for the 4-phase structure
- Native task / Key finding fixes (v2.7.1) — `/tasks add` now explicitly forbids `TaskCreate` for backlog additions (native tasks = current session only, `tasks.org` = long-term backlog); session-start injection states the distinction; "Key finding:" guidance carried into resume + post-compact paths so it survives `/clear` and compaction
- Deferred-work capture + backlog rename (v2.7.2) — sharpened the "suggest deferrable work → ask" rule with concrete trigger phrases and promoted it into SessionStart/resume/post-compact instruction blocks. Assistant-facing text now says "your rpm backlog" (path literals still read `docs/rpm/future/tasks.org`). New **Phase 3a: Clear native tasks** sweeps `TaskList` at session-end — dedups against your backlog (≥80 match → flip TODO→IN-PROGRESS, <80 → append as new TODO), then `TaskUpdate`s all surfaced natives to `completed`. Creation-time was the vetting step, so no user question is asked.
- Version tags + bootstrap UX polish (v2.7.3) — session-start hook reads `plugin.json` via `$CLAUDE_PLUGIN_ROOT` and appends ` (rpm <version>)` to the `session active` / `resuming` / stale-wrap-up headers. session-end scan emits `version=<X.Y.Z>`; Phase 1 header renders as `## Phase 1 (of 4): Collecting Findings (rpm X.Y.Z)`. When `docs/rpm/` is missing, session-start now emits a one-line `/bootstrap` hint to stderr (user terminal, not model context) so the plugin is discoverable — stdout stays empty. `/bootstrap` Phase 7 writes `~rpm-session-start` after scaffolding so runtime hooks (`context-monitor`, `stop-learn-capture`, `pre-compact`, `session-end`, `task-capture`) activate immediately, no restart. `deep-research` description broadened to general research phrasings + Offer gate prompts `quick` vs `deep` before the full multi-agent protocol. README restructured — install + `/bootstrap` as a prominent two-step "Getting started".
- Full opencode port + two-remote git layout + v2.7.6 dual release (2026-04-18). Renamed `origin` → `plugin` (GitHub subtree) and added `dev` → Gitea (york:3333). New `opencode/` sibling: TS plugin bridging opencode's event stream to rpm's existing bash hooks; `scripts/sync-opencode.sh` + `scripts/translate-{skill,agent}.py` mirror skills → commands and translate agents; `scripts/publish-opencode.sh` force-pushes a full .opencode/ tree as a subtree split to `plugin/opencode`; `scripts/publish-all.sh` is the one-shot CC + opencode + tag release. All six rpm slash commands (`/backlog`, `/audit`, `/session-end`, `/init-rpm`, `/rpm`, `/deep-research`) + the `auditor` agent discovered by opencode and end-to-end validated under opencode Zen (`minimax-m2.5-free`, no API key needed). `v2.7.6` tagged on GitHub as the first unified release covering both plugins; users install opencode via `curl -fsSL https://raw.githubusercontent.com/dppdppd/rpm/opencode/install.sh | bash`.
- Deep-research full overhaul (v2.8.0–v2.11.0, tagged v2.11.0 on 2026-04-28). Five plugin releases in one day, all driven by a self-run `/deep-research` against the question "how to best utilize LLMs for deep research." **v2.8.0** — 5 recommendations into both `plugin/skills/deep-research/SKILL.md` and standalone `~/.claude/commands/deep-research.md`: (1) data-only delimiters wrapping every curl-fetched URL (indirect-prompt-injection defense; arXiv:2511.20597 / Comet OTP-leak Apr 2026), (2) deterministic URL liveness pre-check (urlhealth-style; arXiv:2604.03173 cuts non-resolving citations 6–79× to <1%), (3) source-grounded confidence tags — every H/M/L must carry a source URL or be replaced with `Model knowledge — not verified` (GPT-4 AUROC on stated confidence ≈ 62.7%; arXiv:2306.13063), (4) reflection-driven Grader check between rounds (SoK Agentic RAG arXiv:2603.07379), (5) layered citation audit. Also `context-monitor.sh` hard 50k → `WINDOW / 10` proportionate threshold. **v2.9.0** — n=1 A/B experiment confirming W&D pattern (arXiv:2602.07359) cuts ~57% tokens vs 4-subagent on comparison-shaped tasks; wired SURVEY vs DEEP-DIVE task-shape classifier into Phase 1, new SURVEY strategy section in Phase 2 (parallelism-confirmation required), doubled Scaling Rules. **v2.10.0** — inline verify-as-you-write contract in Phase 5b (VeriFact-CoT, arXiv:2509.05741: 72→83% factual accuracy when verification is integrated into writing rather than post-hoc); orchestrator re-Reads relevant fetched-artifact window for every load-bearing claim before writing it. **v2.11.0** — three improvements derived from a DRB2 task8 baseline run (33/52 strict, 40/52 lenient): Phase 2 agent template anchors on user's stated categorization (don't reorganize into popular literature convention), Phase 4 explicit domain-coverage check (region-specific + specialty + niche sources, not just globally popular), Phase 5b URL canonicalization (canonical landing page, not any-2xx — won't 404 in six months). **Released as v2.11.0**: full-tree push to `dev` (Gitea), CC subtree split + opencode subtree split + tag pushed to `plugin` (GitHub) via `scripts/publish-all.sh`. New memory: `feedback_cd_amp_precedence.md` (`cd $dir && cmd1 & cmd2 & ...` parses as `(cd && cmd1) &`; only cmd1 sees cd context). Research artifacts on disk: `research/llm-deep-research-best-practices/` (the canonical report driving v2.8.0), `research/wd-vs-multiagent-citation-grounding/` (the v2.9.0 A/B experiment), `research/drb2-baseline/` (v2.11.0 evidence + scaffolding for future task runs), `research/managed-agents-scoping/` (decision: NO migration to Anthropic Managed Agents; 4 reasons + 3 revisit conditions documented), `research/query-count-ablation/` (deferred — Grader check obsoletes the fixed-budget question; methodology stub for any future run).
- Backlog gap-mining sprint (v2.18.0–v2.18.2, 2026-05-26). Three releases in one day driven by transcript mining (Claude across 9 projects + 267 Codex sessions, 33 platform-tagged gaps, 8 prioritized fix tickets). **v2.18.0** bundled the slam-dunks: `context-monitor.sh` atomic counter (eliminates volta's 543+ stderr crash signature with `flock`-free defensive read + atomic mv write), `scan.sh` unified plugin-root resolver chain + `--exclude-dir={node_modules,.opencode,.venv,…}` on NOT_IMPLEMENTED grep (fixes Codex `version=unknown` and the .opencode/node_modules 3.6MB output blow-up), `/backlog` + `/next` SKILL.md `TRIGGER` blocks (mirrors `/deep-research`'s description shape) for natural-language verb discoverability, Stop-hook backfill for `task: (unassigned)` (uses commit subject with conventional-prefix strip, or basenames) + session-start `top_actionable` fallback chain, auditor `Phase 0: Noun cross-check` (extract high-impact nouns from CLAUDE.md/status.md, grep against code excluding docs/, tag findings as `doc-stale`/`doc-orphan`/`code-undocumented`, surface doc-orphans first), `/next` F1-F4 (WATCH keyword in `#+TODO:`, today-date dispatch field, `is-pre-completed.sh` skip helper, no-ops counter), codex SessionStart context mirror (tee + EXIT-trap writes `docs/rpm/~rpm-context.md`; AGENTS.md `# include` makes it discoverable in Codex), and review-ready/status `agent_id` pairing fix (drop `agent_id` from `(target, agent_id)` join — workers default to `worker-unknown` while orchestrator records dispatch IDs, join never matched). bats 132 → 175. **v2.18.1**: `/init-rpm` Phase 4a — unified `repair.sh` helper covers two stale-config migrations: `.gitignore` missing `docs/rpm/~rpm-*` wildcard (with opt-in collapse of accumulated per-file entries; this repo had 10 of them) and `AGENTS.md` missing `# include: docs/rpm/~rpm-context.md` (frontmatter-aware prepend). Wired into Repeat-run verification mode. 12 new bats. Codex mirror byte-identical via `translate-skill-codex.py` (which itself was patched mid-publish when the `/next` TRIGGER block sync revealed a hardcoded-description drift). Dogfooded — `.gitignore` here is now 1 wildcard line. **v2.18.2**: CI fix for the previous two. `auditor-phase-0.bats` + `no-placeholder-paths.bats` hard-referenced `$REPO_ROOT/codex/...` paths that resolve in the monorepo but break on the subtree-split plugin remote (no codex/ sibling). 7 failing tests on v2.18.0 + v2.18.1 CI; v2.18.2 lifts the `require_codex_port_layout` pattern from `codex-port.bats` and conditionally skips codex assertions. Verified by simulating subtree layout (`cp -r plugin /tmp/rpm-ci-sim && bash tests/run.sh` → 187/187) before publishing. GitHub Actions confirmed green on v2.18.2.
- /next preflight delegation + amendments / contradiction checks (v2.19.0–v2.20.0, 2026-05-29). **v2.19.0**: project-amendment convention (`docs/rpm/skills/<name>.md` adds project-specific instructions to a skill without forking it), memory↔docs contradiction checks (lightweight at `/next` preflight via `guidance-aligner` contradictions-only mode, heavyweight at `/session-end`), and override-drift detection in `scan.sh`. **v2.19.1**: override detection extended to `.claude/commands/<name>.md` (not just `.claude/skills/`). **v2.20.0**: `/next` delegates its token-heavy preflight (mechanical-drift scan + fixes, contradiction check, worker review) to a new `rpm:preflight` subagent so raw `scan.sh` output, contradiction reasoning, and worker diffs stay in the agent's context, not the orchestrator's. Single-turn model preserved (one `/next` = full preflight + decision); only the context cost moves into the agent, which returns a ~6-field compact report and logs its own `drift-fix`/`review-result` entries. New `plugin/agents/preflight.md`; SKILL preflight collapses 3 inline steps into 1 delegated step (net −40 lines); codex mirror wired via `AGENT_TO_SKILL`; 7 new bats (206 total).
- Backlog ordering + session-end evolution (v2.7.7–v2.7.9, 2026-04-21). Three consecutive releases. **v2.7.7**: reframed `tasks.org` as sorted in **execution order** (top = next to do, not an abstract priority score; "chronological" = when work happens, not when created). Added 4-band sort per `* Parent`: Actionable → Blocked → Postponed → Historical, with blocked/postponed auto-drifting to the bottom of their band on every write. Memory `feedback_tasks_execution_order.md`; one-time `tasks.org` re-sort (15/15 IDs preserved). **v2.7.8**: added a **pivot-capture rule** in `plugin/hooks/_directives.sh` + backlog SKILL preamble + `feedback_pivot_capture.md`: on mid-session pivot to TaskCreate-worthy work, LLM auto-updates `~rpm-session-start` `task:` AND inserts `** TODO` at top of actionable band — no user question. Keeps the backlog truthful even on abnormal session exits. **v2.7.9**: trimmed `/session-end` — killed the Phase 1 "Actions" outer menu (redundant with Phase 2 sub-prompts, which already fire per-surface) and added a **fast-path** single-response mode for clean sessions (drift/learnings/mismatch + natives all empty + git clean → one `## Session end (rpm <version>)` block with Phase 4 cleanup inlined). Full 4-phase flow preserved as fallback. Backlog follow-ups filed: `publish-all-push-dev` (auto-push dev in publish-all.sh) and `pivot-capture-hook` (deferred hook-side enforcement).
- deep-research citation hardening + native bake-off + eval harness (2026-05-30, in-tree, unreleased). Controlled bake-off (`docs/rpm/research/dr-bakeoff/`) found native `/deep-research` matches rpm on rigor — so rpm's differentiator is orchestration (portability / durable artifacts / inline / cost-caps), not output quality. Hardened `deep-research/SKILL.md` (number-provenance gate, kill-list + "Could not verify" section, quantitative source-tiering, active-refutation + default-to-kill verify); built offline citation auditor (`checks/offline_audit.py`, 56% orphan baseline, ~0 tokens) + Tier-2 replay validating the fix kills ƒ134M/ƒ120M. Commits `2ba5fcc`, `f5ab7fa`. Backlog: verification-chain hardening, diverse-verify, Module B injection, Tier 3/4 remain.
- deep-research verification-chain hardening + #4 rescope (2026-05-31, in-tree, unreleased). **`997c530`** — #2/#3/#5: silent-drop instrumentation, number-provenance figure-ledger gate (every figure literally in a fetched source or killed), post-synthesis `synth-introduced figures = 0` assertion; validated by a blind Tier-2 replay over voc-decline (recall 21/21 incl. the ƒ134M crux, over-kill 0/4, 3/3 grader agreement). **`3dec022`** — #4 perspective-diverse 5-lens panel, **rescoped**. An n=2 blind over-kill A/B/C measurement (`docs/rpm/research/dr-bakeoff/overkill-2026-05-31/`; voc-decline tertiary + draftyard primary, 14 claims) showed **over-kill tracks source tier, not voter correlation** — #4's "over-kill ↓" eval is false; its measured benefit is recall on present-but-wrong claims (panel 2/2 vs weakest single lens 1/2). Committed with eval reframed + provenance absent-vs-tertiary calibration (numbers still die per Principle 3). Backlog #2/#3/#4/#5 closed; Module B injection + Tier 3/4 remain. New eval asset: the over-kill harness joins the dr-bakeoff regression corpus.

## Active Specs

## Known Issues
