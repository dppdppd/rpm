# 2026-05-26

## Session: Mine Claude+Codex histories for rpm gaps
- **Session:** 5f698660-2205-40b1-ac1c-7960606a82e9
- **Mode:** /session-end (phased)

### Accomplished
- Three-round transcript mining across 9 Claude projects (~1255 jsonls) +
  267 Codex sessions surfaced 33 rpm gaps, each tagged by platform
  (CC-only / Codex-only / Both / Structural).
- Filed 8 prioritized fix tickets at top of `docs/rpm/future/tasks.org`,
  ordered slam-dunks → design-decisions, with one detail file per ticket
  in `docs/rpm/future/2026-05-26-*.md`.

### Key Discoveries
- **Codex SessionStart stdout is invisible**: hook runs, output drops on
  the floor — only stderr surfaces. Combined with Codex's structural lack
  of SessionEnd/PreCompact/PostCompact/TaskCreated hooks, most of rpm's
  session-context discipline is absent in Codex. 0/63 Codex sessions show
  the rpm banner in conversation.
- **`context-monitor.sh` race condition**: non-atomic read-modify-write on
  the counter file. **543+ stderr crashes across 8 volta sessions**, peak
  198 crashes in one session (`c06be4a7-...jsonl:1690`).
- **`/backlog` bypassed at >99%**: direct-edit-to-skill ratio is 522:3 in
  Claude across all sampled projects, 103:0 in Codex. The skill's
  description doesn't claim natural verbs ("backlog X", "add to backlog").
- **`/session-end` is invoked MORE in Codex** (~30%) **than in Claude**
  (~2%). Intuition inverted: Codex sessions are smaller and more
  transactional; Claude sessions persist across days and die without
  ceremony.
- **volta marker `task: (unassigned)` stuck for 27+ days** surviving
  multiple CC and Codex sessions — neither runtime backfills it when the
  session dies without /session-end. 215 propagated occurrences in
  Codex; 240 instances of `(unassigned)` in CC SessionStart previews.
- **Hardcoded `claude-plugins-official/rpm/unknown/` placeholder** ships
  in skill bodies. Assistants bash a nonexistent path before
  find-recovering. `unknown` looks like an unrendered template segment.
- **scan.sh emits `version=unknown` in Codex** (47 occurrences) because
  `${CLAUDE_PLUGIN_ROOT}` is unset and scan.sh has no Codex-side resolver.
- **SessionStart hook output truncation**: 152KB peak in volta. Combined
  with Codex's stdout-invisible reality, this is CC-only effective issue.
- **Auditor confidently surfaces findings against stale docs without
  cross-checking live code**: one reddit-reports auditor run produced 13
  wrong findings about a decommissioned VPS.

### What Didn't Work
- Initial assumption that Codex `/loop` doesn't exist — wrong; `/loop`
  comes via Anthropic loop skill which is cross-host (655 occurrences in
  Codex history.jsonl).
- Initial inventory missed that BGSD/BIT actually DID bootstrap mid-session;
  the "installed-but-unbootstrapped" sample was much smaller than
  expected. counter-tray-designer + docker-coder (6 sessions) became the
  real evidence for "stderr hint ignored at scale".

### Next
- Fix context-monitor.sh race condition (top of backlog — CC, CRITICAL
  slam-dunk, ~1-line diff with atomic-rename pattern).

## Session: Execute backlog — /loop /next closes 8 of the mined tickets
- **Session:** 5f698660-2205-40b1-ac1c-7960606a82e9 (same)
- **Mode:** /loop /next then /session-end (phased)

### Accomplished
- 8 backlog tickets DONE in one autonomous run via `/loop /next`,
  comprising 4 background general-purpose worker dispatches + 3
  inline orchestrator completions + 1 already-implemented lock-in:
  1. **context-monitor-race** — atomic-write + defensive-read fix to
     `plugin/hooks/context-monitor.sh:28-30`; eliminates the volta
     crash signature (`syntax error in expression (error token is …)`).
  2. **scan-version-and-excludes** — unified plugin-root resolver chain
     in `scan.sh` (env vars → monorepo relatives → Codex cache glob);
     adds `--exclude-dir={node_modules,.opencode,.venv,.git,dist,
     build,target}` to NOT_IMPLEMENTED grep. Codex `version=unknown`
     gone, 3.6MB output blow-up gone.
  3. **strip-unknown-plugin-paths** — no-op (already absent from
     shipped trees) + new `plugin/tests/no-placeholder-paths.bats`
     guard so it never comes back.
  4. **backlog-discovery-verbs** — TRIGGER block added to
     backlog SKILL.md description with natural-language verbs
     (`backlog X`, `add to backlog`, `postpone N`, `done N`, …).
     Same shape as `/deep-research`'s description. Mirrored to Codex.
  5. **next-discovery-verbs** — TRIGGER block on `/next` description
     for terse forms (`next`, `next?`, `what's next`, `keep going`,
     `continue`). Mirrored to Codex (runtime wording divergence
     preserved).
  6. **task-unassigned-backfill** — `stop-learn-capture.sh` backfills
     `task: (unassigned)` from git commit subject or modified-file
     basenames; `session-start-auto.sh` fallback chain
     (`top_actionable` → `next:` → `(no task recorded)`) prevents the
     placeholder from leaking into resume nudges.
  7. **auditor-code-crosscheck** — `plugin/agents/auditor.md` Phase 0
     noun cross-check + three-tag taxonomy
     (`doc-stale`/`doc-orphan`/`code-undocumented`) + doc-orphan-first
     report ordering. Stops the auditor producing confident findings
     against drifted docs.
  8. **next-refinements** — F1 `WATCH` keyword in
     `#+TODO:` (skipped by task-selection), F2 `no-ops:` counter
     verified in `status.sh` (already present), F3 `today: <date>`
     field in Worker Contract dispatch, F4 `is-pre-completed.sh`
     helper that exits 0 when a task's detail file already has
     `## Worker Result`.
- bats suite grew 132 → 160 (28 new tests). Codex mirrors verified
  byte-identical for every shared file (context-monitor.sh, scan.sh,
  auditor.md, stop-learn-capture.sh, session-start-auto.sh,
  is-pre-completed.sh). All shellcheck-clean.
- 3 BLOCKED items review-acknowledged with `approved-as-blocked`
  rationale:
  - `pivot-capture-hook` — wait for LLM-side drift evidence.
  - `phase3-retry-hardening` — wait for rate-limit / Cloudflare /
    batch-timeout event in deep-research.
  - `codex-sessionstart-strategy` — moved BLOCKED → TODO after user
    chose option B (transient `~rpm-context.md` + AGENTS.md include).
    Decision recorded in the detail file under
    `## Decision (2026-05-26)` with explicit implementation surface.

### Key Discoveries
- **Loop-mode "no actionable" needs an escape valve.** When the top
  topmost TODO carries explicit deferral text ("DECISION NEEDED",
  "Defer until observed", "Hasn't been observed in practice"),
  treating it as actionable would mean autonomously guessing the
  user's design call. The right move is to flip to BLOCKED so the
  loop walks past it instead of idling on the same entry until
  `loop-exhausted`. This is now the practical pattern when the
  /next orchestrator hits a soft-deferred top item — earlier
  fixes (`WATCH` keyword) standardize this on the next cycle.
- **F4's pre-completed-task guard locks in retroactively.** The new
  `is-pre-completed.sh` now exits 0 for every detail file touched
  this session — future `/next` ticks will skip the wasted dispatch
  if any DONE flip is ever missed in tasks.org.
- **Worker `agent_id` drift.** Workers default to
  `worker-unknown` because runtime doesn't pass an agent ID to the
  subagent prompt. The orchestrator logs the dispatch ID; the worker
  logs `worker-unknown`. `review-ready.sh` joins by target+agent_id,
  so reviewed entries can still surface as "needs-review" until the
  agent-id pairing is fixed. Worked around inline by logging
  review-result entries; the underlying script bug is a follow-up.

### What Didn't Work
- Initial loop attempt to dispatch a worker on
  `codex-sessionstart-strategy` would have silently committed to one
  of three architectural options without user input — caught by
  re-reading the detail file's "DECISION NEEDED" header and flipping
  to BLOCKED instead. Confirms the loop-mode pattern above.

### Next
- Implement option B for `codex-sessionstart-strategy`
  (`session-start-auto.sh` writes transient `~rpm-context.md`,
  AGENTS.md `# include`s it, session-end deletes it, `.gitignore`
  entry, bats coverage). Detail file's "Decision (2026-05-26)"
  section has the implementation surface ready.
