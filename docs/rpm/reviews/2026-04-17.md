# rpm Review — 2026-04-17 (full report)

## Scope

Full consultant review. Target: rpm plugin at /home/coder/projects/rpm, v2.7.2 (plugin.json:2).

Phases followed (per `plugin/skills/audit/project-mode.md`):

1. Investigate — git history, all hooks, all skills, tests, CI, memory
2. Inward research — Claude Code plugin hook spec + marketplace schema
3. Outward research — ccpm, session-kit, clauditor, claude-hud, ultimate-guide
4. Analyze — 7 dimensions + gap analysis
5. Deliverables (this file + plan + log line)

Hard gate respected: `rpm:auditor` subagent ran in parallel; its 6 findings merged below.

## Evidence snapshot

| Metric | Value |
|---|---|
| Plugin version | 2.7.2 |
| Test suite | 82 passing, 0 failing (bats) |
| Shellcheck on hooks/ | 0 warnings (via CI) |
| Shellcheck on skills/**/*.sh | 5 warnings, unlinted in CI |
| CLAUDE.md | 35 lines (under 150-line limit) |
| Skills | 6 (audit, bootstrap, deep-research, rpm, session-end, tasks) |
| Hooks | 8 (SessionStart, SessionEnd, PreCompact, PostCompact, PostToolUse, Stop×2, TaskCreated/Completed) |
| Agents | 1 (auditor, sonnet) |
| Memory files | 20 (17 feedback + 3 reference) |
| Most-changed file (last 30 commits) | `plugin/skills/session-end/SKILL.md` (13 touches) |
| Auditor scan findings | 6 (5 at conf ≥60, 1 suppressed at 38) |

## Dimensional analysis

### 1. Process Health
Plan → edit → verify → commit is followed. Solo dev with one-commit-per-deliverable hygiene. Session `a4ee0e19` (Apr 13–16) broke the session-end discipline — ran 4 days + 9 versions + 2 unclean exits. That's a discipline hiccup but not a systemic issue (it's the only recent long-session incident; scan.sh detected the stale marker and flagged it at session-start of the next session).

### 2. Architecture & Code Health
Plugin structure is clean and matches documented architecture (`CLAUDE.md:12-20`). No circular deps, no orphan scripts.

One structural concern: `skills/session-end/scripts/scan.sh` is 584 lines with 10 distinct sections. Each section parses a different corner of the project state. The sections are well-delimited (`=== name ===` headers) but there's no modular decomposition — one bash file grows monotonically. No refactor needed now; flag for later if it passes ~800 lines.

### 3. LLM Workflow
CLAUDE.md at 35 lines is well under the 150-line ceiling. Good structural ratio (83% structured per auditor). 17 feedback memory files — most codified into CLAUDE.md, context.md, skills, or hooks. Two gaps in codification:
- `feedback_consolidate_parallel_bash` — memory only (see Plan Task 6).
- `feedback_verify_version_before_tag` — partial; lives in context.md Focus Areas + a past log Key Discoveries, but not as a standing CLAUDE.md rule.

Session-end SKILL.md at 428 lines is the heaviest skill body. Already uses scan.sh prep to offload mechanical work, but the instruction prose is long. Acceptable for now given the 4-phase structure; flag for tightening if it grows past ~500 lines.

Phase 3a scoring (see Plan Task 7) re-derives logic that already exists in task-capture.sh — minor token duplication.

### 4. Risk & Compliance
- CI gap: shellcheck misses `skills/**/*.sh`. scan.sh (load-bearing) has 5 local shellcheck warnings. (Plan Task 3.)
- Test gap: no direct bats for scan.sh. (Plan Task 4.)
- Marketplace.json is correct (object-form `source`, fixed in v2.5.9).
- CI runs on plugin-only branch after subtree split; dev branch (local master) has no CI trigger since `.github/` lives under `plugin/`. Intentional for solo dev; call out only if someone joins the project.

### 5. Strategic Direction
The top backlog task — **Submit latest to anthropics/claude-plugins-official marketplace** — has been at position 1 since 2026-04-13 without movement across 9 version bumps. Two readings:

- **It's actually blocked** — marketplace form behavior / pinning concerns / waiting on something.
- **It's deprioritized implicitly** — real focus is on the port tasks (Codex, opencode) + polishing session-end.

Either way, parking it silently at the top is worse than either surfacing the block or moving it down. Consider a `:BLOCKED_BY:` property or a `/tasks postpone` at the next session-end Phase 3b.

### 6. Session Discipline
Scored mostly clean:
- status.md drifts by 2 days (Plan Task 1).
- tasks.org body drifts 8 versions (Plan Task 1).
- 2026-04-13 → 2026-04-16 past/ logs are exit stubs only (Plan Task 2).
- past/log.md Sessions Reviewed stops at Apr 12 (Plan Task 5).

Each is individually small. Together they form a pattern: when a long session doesn't wrap with `/session-end`, every downstream tracker drifts simultaneously. The new SessionEnd hook (`session-end.sh`) catches this and stubs past/, but doesn't backfill the other trackers. Not a bug — a design seam.

### 7. Competitive Gaps (outward)

| Tool | What they do | What to adopt | Verdict |
|---|---|---|---|
| **ccpm (automazeio/ccpm)** | GitHub Issues as source of truth + git worktrees for multi-agent parallel exec | Bundle-ops-as-scripts pattern (14 scripts for standup/status/next/blocked) | Reject wholesale (rpm targets solo dev); **borrow the script-bundle pattern** for Phase 3a scoring (Plan Task 7). |
| **claude-code-session-kit (shihchengwei-lab)** | 4 hooks, 40/60/70% context alerts, hard stop at 70% | handoff-check pattern (already borrowed in handoff-validator.sh:5) | Already borrowed the pattern; hard-stop explicitly rejected in v2.5.6. |
| **clauditor (IyadhKhalfallah)** | Auto-rotate oversized sessions, emergency preservation | — | Reject. rpm's PreCompact/PostCompact pair is cleaner than forced rotation. |
| **claude-hud (jarrodwatts)** | Real-time HUD (context, tools, agents, todos) | — | Complementary, not competitive. rpm doesn't do UI. |
| **FlorianBruniaux/claude-code-ultimate-guide** | Guide + audit-prompt.md | audit-prompt.md phrasing ideas | Marginal. Our `/audit project` is richer than their audit-prompt. |

### 8. Gaps & Opportunities
- **Script-bundle pattern** for Phase 3a (Plan Task 7).
- **Scan.sh modularization** — if scan.sh keeps growing, consider splitting into `scan.d/` with one file per section and a driver. Not today.
- **Temporary-disable switch** — no documented way to run CC in this project without rpm loading. Delete `docs/rpm/` works but is destructive. Minor; only flag if asked.

## Findings (merged — manual investigation + auditor)

| # | Finding | Severity | Confidence | Source | Plan Task |
|---|---|---|---|---|---|
| 1 | tasks.org marketplace-submit body is 8 versions stale | Medium | 82 | auditor F1 | T1 |
| 2 | status.md says `69 tests` — actual is 82 | Medium | 80 | manual | T1 |
| 3 | CI shellcheck doesn't cover skills/**/*.sh | Medium | 80 | manual | T3 |
| 4 | past/2026-04-16.md is exit stubs only — v2.5.5–v2.7.2 work undocumented in prose | Medium | 79 | auditor F2 | T2 |
| 5 | status.md "Last updated" is 2 days stale | Medium | 72 | auditor F3 | T1 |
| 6 | scan.sh unlinted + no direct bats coverage | Medium | 70 | manual | T3, T4 |
| 7 | past/log.md Sessions Reviewed stops at 2026-04-12 | Low | 68 | auditor F4 | T5 |
| 8 | feedback_consolidate_parallel_bash not codified | Low | 63 | auditor F5 | T6 |
| 9 | Phase 3a native-scoring duplicates task-capture.sh logic | Low | 60 | manual | T7 |

Suppressed (below 60 threshold):

- CLAUDE.md uses short hook labels (`session-start`, `stop-learn`) vs filenames (`session-start-auto.sh`, `stop-learn-capture.sh`) — auditor F6, conf 38. Labels are conceptual, not pathnames; hooks.json wires the real paths.
- Competitive gap: no GitHub Issues mode (ccpm feature) — conf 40. Deliberate design.
- Instruction debt from 20 memory files — conf 40. Audit-alignment table shows most are codified.

## Strategic note (not a finding — requires developer call)

`marketplace-submit` at position 1 since Apr 13, 9 versions stale. Decide at next `/session-end` Phase 3b whether to:
1. Attempt the submission (would complete the task + reset the pin).
2. Apply `/tasks postpone marketplace-submit` to move below port tasks.
3. Add `:BLOCKED_BY:` if there's an actual blocker.

Current behavior (silently sitting at top) drifts the `~rpm-last-session next:` field, which cascades into every new session's opening line.
