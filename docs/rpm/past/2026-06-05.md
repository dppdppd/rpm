# 2026-06-05

## Accomplished
- Ported the goal-seeking "PROOF step" discipline upstream into the plugin
  (generalized from Volta's amendment Step 1.0):
  - `next/SKILL.md` Task Selection step 4 — before dispatching toward a NOT-MET
    parent goal, demand a demonstrable definition-of-done + a terminal
    verify/sign-off task; if either is missing, **filing it IS the dispatch**.
    Then fall through to the existing goal-aligned dispatch.
  - `backlog/SKILL.md` Review — new **Demonstrable goals** check flags any
    parent whose `Goal:`/`Success:` is a gap-list/vague description or whose
    `[NOT MET]` chain has no terminal proof task.
  - Released **v2.22.0** (CC `master` + opencode + codex + tag).
- Made `/backlog add` proactive about goals + research (follow-on user requests):
  - propose a demonstrable `Goal:` when a new `* Parent` group is created
    (skip status bands / catch-all buckets);
  - offer the `research` skill (`rpm:research`) when a task crosses into a new
    domain, hinges on external unknowns, or is a selection/comparison (offer
    only, never auto-run; skip mechanical/internal work).
  - Released **v2.23.0** (CC `master` + opencode + codex + tag).
- Both ports synced via `sync-codex.sh`; 206/206 bats green before each release.

## Key Discoveries
- The goal-seeking arc now spans the whole backlog lifecycle: **enforce**
  (next dispatch) → **flag** (backlog review) → **propose + steer** (backlog add).
- codex skills auto-translate from `plugin/` (no manual sentinel on next/backlog);
  opencode carries only the `rpm` skill, so next/backlog need no opencode mirror.

## What Didn't Work
- Nothing notable — clean session, dry-run validated the release pipeline before
  the first push.

## Next
- v1/v2/native deep-research bake-off on a Dutch-primary-source VOC topic
  (top actionable backlog item).
