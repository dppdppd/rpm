# rpm Plan — 2026-04-17

## Context

Full consultant review of rpm v2.7.2. Investigation covered:

- Git log (30 commits), working tree, CLAUDE.md (35 lines), context.md
- All 8 hooks, all 6 skills, agents/auditor.md
- scan.sh (584 lines), plugin.json, marketplace.json
- bats suite (82/82 passing) + CI workflow
- 20 memory files (17 feedback + 3 reference)

Research conducted:

- **Inward — Claude Code plugin hook surface (code.claude.com, 2026)**. Confirmed all 8 rpm hooks (SessionStart/End, Pre/PostCompact, PostToolUse, Stop, TaskCreated/Completed) are current. 15+ hook events now documented; rpm uses a consistent subset with no deprecations.
- **Outward — ccpm (automazeio/ccpm)**. GitHub Issues + git worktrees for multi-agent parallel exec. Complementary, not adoptable (rpm targets solo dev). Their bundle-ops-as-scripts pattern informs Task 7.
- **Outward — claude-code-session-kit (shihchengwei-lab)**. rpm already draws its handoff-validator from session-kit; session-kit's 40/60/70% hard-stop was deliberately rejected in v2.5.6. Design choice stands.

Findings merged with background `rpm:auditor` subagent scan (6 findings; F6 suppressed at conf 38).

## Tasks

### Task 1: Refresh stale tracker metadata (bundle)
- **Severity:** Medium
- **Dimension:** Session Discipline
- **What's wrong:** Three metadata fields pin pre-v2.7 state:
  - `docs/rpm/future/tasks.org:10` — marketplace-submit body says `Latest: SHA 59a47b5 (tag v2.5.4)`; actual is v2.7.2.
  - `docs/rpm/present/status.md:30` — `(plugin/tests/, 69 tests — full hook coverage)`; actual is 82 tests.
  - `docs/rpm/present/status.md:5` — `Last updated: 2026-04-13`; file was committed 2026-04-15 (330fbf8).
- **Why it matters:** Leaks into session-start briefings. The handoff text written this session literally echoed the v2.5.4 pin back to the user because the marker read from the stale task body.
- **Research says:** Sitepoint / Anthropic context-engineering docs call out "instruction debt" — stale rules displace useful ones and compound per-session token cost.
- **Fix:** One commit editing all three lines. Verify with `grep -n "v2.5.4\|69 tests\|2026-04-13" docs/rpm/` after.
- **Effort:** Small

### Task 2: Backfill past/ for v2.5.5–v2.7.2 window
- **Severity:** Medium
- **Dimension:** Session Discipline
- **What's wrong:** Session `a4ee0e19` ran 2026-04-13 → 2026-04-16, shipped v2.5.5 through v2.7.2 (context-monitor real-tokens rewrite, sidechain filter, single-option menus, marketplace.json object form, session-end 4-phase overhaul, Phase 3a sweep, backlog rename). Exited twice without `/session-end`. `past/2026-04-16.md` contains only two exit-stub entries written by the SessionEnd hook.
- **Why it matters:** 9 versions of substantive work is absent from past/ prose. Next audit or handoff has to reconstruct "why" from commit messages alone.
- **Research says:** Aroussi on ccpm — "mechanical extraction preserves files and commits; Claude's prose captures reasoning the transcript can't". Commit subjects ≠ retrospective.
- **Fix:** Draft Accomplished / Key Discoveries / Next retrospective for 2026-04-13 through 2026-04-16 using `git log --stat` + status.md v2.5.5–v2.7.2 bullets as evidence. Append to `past/2026-04-16.md` (or split across dates if commit timestamps cluster clearly).
- **Effort:** Medium

### Task 3: Extend CI shellcheck to skills/**/*.sh
- **Severity:** Medium
- **Dimension:** Risk & Compliance
- **What's wrong:** `plugin/.github/workflows/test.yml` runs `shellcheck hooks/*.sh || true`. Misses `skills/session-end/scripts/scan.sh` (584 lines, load-bearing) and `skills/bootstrap/scripts/detect.sh`. Local shellcheck on scan.sh surfaces SC2010 (ls-grep antipattern), SC2086 (quoting), SC2154 (eval'd var references), SC2016 (false positive on sed patterns). All minor but real.
- **Why it matters:** scan.sh output drives every `/session-end` and `/audit quick`. A regression is silent until a session-end mis-reads. Hooks are linted; skills/ scripts running at the same lifecycle privilege should be too.
- **Research says:** Claude Code hook-development skill recommends shellcheck for all plugin bash. ccpm lints its entire script tree.
- **Fix:** Change workflow to `shellcheck hooks/*.sh skills/*/scripts/*.sh || true`. Optionally address SC2010 + SC2086 in scan.sh as part of the same commit (short pass).
- **Effort:** Small

### Task 4: Add direct bats coverage for scan.sh
- **Severity:** Medium
- **Dimension:** Risk & Compliance
- **What's wrong:** scan.sh is load-bearing + 584 lines + non-trivial section parsers (task_deps, specs_inventory, broken_refs, pm_docs_staleness, migration). No dedicated bats file. Coverage today is indirect via session-end-hook.bats.
- **Why it matters:** Section formats evolve across versions. Downstream skills (session-end, audit) depend on the `key=value` contract. A format drift in one section will be caught only when a session-end mis-processes.
- **Fix:** New `tests/scan.bats` covering: git section (clean/dirty), broken_refs true positive + false positive, task_deps with dangling refs + ready tasks, specs_inventory with and without spec dir, migration detection against each rename pair.
- **Effort:** Medium

### Task 5: Log Sessions Reviewed for a4ee0e19 + 2026-04-17 audit
- **Severity:** Low
- **Dimension:** Session Discipline
- **What's wrong:** `past/log.md` Sessions Reviewed stops at 2026-04-12. a4ee0e19 (Apr 13–16, 9 versions) and the current audit session are unreviewed.
- **Fix:** Append rows classifying a4ee0e19 as JUSTIFIED (deliberate feature work; past/ gap is F2's concern) and the current audit as N/A. Covered as part of this audit's log write.
- **Effort:** Small

### Task 6: Codify feedback_consolidate_parallel_bash in CLAUDE.md
- **Severity:** Low
- **Dimension:** LLM Workflow
- **What's wrong:** Memory rule "parallel Bash calls cancel each other on error; bundle into single scripts" lives only in MEMORY.md — not in CLAUDE.md, context.md, or any skill body. Memory isn't guaranteed to load on session start.
- **Why it matters:** The `no-ADRs` and `read-before-renaming` guardrails made it into CLAUDE.md; same logic applies here. One-off learnings cached in memory don't survive when memory fails to hydrate.
- **Fix:** Add one line to `CLAUDE.md` Guardrails: `- Parallel Bash calls cancel each other on error — bundle into a single script`.
- **Effort:** Small

### Task 7: Move Phase 3a native-scoring into a bash script (defer)
- **Severity:** Low
- **Dimension:** LLM Workflow (token cost)
- **What's wrong:** `/session-end` Phase 3a instructs Claude to normalize + score in-progress/pending natives against tasks.org headings. The exact `normalize()` and `confidence()` logic already lives in `plugin/hooks/task-capture.sh:59-87` (45 LOC). Phase 3a re-derives it mentally per session.
- **Why it matters:** Tokens plus risk of cross-session scoring drift.
- **Research says:** ccpm's 14-script bundle (per project-mode.md:121) is the explicit pattern for exactly this — bundle deterministic ops as bash. The scoring logic is already deterministic and factored.
- **Fix:** Extract `skills/session-end/scripts/score-natives.sh` reading TaskList JSONL stdin, emitting `{heading, id, confidence}` lines. Phase 3a becomes: TaskList → pipe → act. task-capture.sh can source the same library.
- **Effort:** Medium
- **Note:** Defer. Current approach works; optimization, not fix.

## What's Working (don't break these)

- Soft-nudge philosophy — no hard stops on wrap-up; deliberate reject of session-kit's 70% hard-stop.
- Paired session markers (`~rpm-session-start` + `~rpm-session-end`) with session_id pairing for deterministic orphan detection.
- Scan.sh injection pattern — Phase 1 mechanical data lands in context without a tool call.
- 82/82 bats passing + GitHub Actions CI (on plugin-only branch via subtree split).
- Memory system discipline — 20 files, cleanly classified feedback / reference.
- Bootstrap's Phase 6 permissions prompt — one ask, not per-file.
- Solo-dev-first design — rejecting ccpm's GitHub Issues model is correct for the target.
- "Key finding:" carried across startup + resume + post-compact paths (v2.7.1).
- Assistant-facing "your rpm backlog" phrasing (v2.7.2).
