# 2026-05-31 — rpm

## Accomplished
- **deep-research verification-chain hardening (#2/#3/#5)** → committed `997c530`.
  Three enforceable gates in `plugin/skills/deep-research/SKILL.md`: silent-drop
  instrumentation (log every dropped claim + drop tally), number-provenance figure-ledger
  gate (every figure must literally appear in a fetched source or be killed), and a
  post-synthesis assertion (`synth-introduced figures = 0`). Validated by a **blind Tier-2
  replay** over the frozen voc-decline corpus: recall 21/21 on unsupported figures (incl.
  the ƒ134M crux), over-kill 0/4, 3/3 agreement with the deterministic grader. Orchestrator
  independently confirmed (219M verbatim in Gutenberg; 134M/120M absent).
- **deep-research perspective-diverse verification (#4)** → committed `3dec022`, **rescoped**.
  Added a 5-lens panel block, then ran an n=2 blind over-kill A/B/C measurement before
  trusting it. Rescoped the eval from the measured-false "over-kill ↓" to the measured-true
  "recall on present-but-wrong claims ↑"; kept the OR-kill panel rule (vindicated); added a
  provenance absent-vs-tertiary calibration so OR-kill can't over-veto a true qualitative
  claim on weak sources.
- Closed backlog #2/#3/#4/#5; corrected the improvement table (#2/#3/#5 had shipped in
  997c530 but still read TODO). Updated `feedback_grade_own_tools_hard` memory with
  blind-measurement + n≥2-replication discipline.
- **Module B injection — full investigation + Principle 8 ditched.** Ran a measured
  2-model × 2-surface study (~46 blind trials, ground-truth logs): rpm's Principle-8
  "treat as data" wrapping + strip-list reduce injection in **0/4 contexts** (redundant on
  Opus; ineffective on Haiku synthesis — injected token appended 3/3 wrapped = 3/3 raw;
  agentic 0/13 P8 vs 1/13 native = noise). Removed both from `deep-research/SKILL.md`
  (→ documentation line; figure-ledger gates untouched; suite green 206). Recorded the
  full matrix + reusable harness in the improvement-plan detail file + scorecard; backlog
  #7 → DONE; filed `canary-grader-obey-fix` + `injection-weak-model-arm`.

## Key Discoveries
- **Over-kill in deep-research verification tracks SOURCE TIER, not voter correlation.**
  Measured across two domains (voc-decline tertiary-sourced history; draftyard
  primary-sourced software-UX), 14 claims, zero clean over-kill canary. With tertiary
  sources provenance over-kills (a Principle-3-correct refusal); with primary sources
  over-kill is 0/6 everywhere. Perspective-diversity does not reduce it.
- **Perspective-diversity's real payoff is recall on present-but-wrong claims:** on draftyard
  the methodology + alternative-hypothesis lenses each caught a referent-stretch AND a ranking
  contradiction that provenance/consistency/recency missed (panel 2/2 vs weakest single lens
  1/2) — a #3-metric gain, not #4's claimed over-kill one.
- The committed OR-kill panel rule is optimal on good sources, inert on weak (provenance
  dominates the kill set). Majority-voting cuts over-kill only by collapsing orphan recall
  4→1. The real over-kill lever is the provenance absent-vs-tertiary split.
- A structural "validation" that grades a gate with the gate's own logic is tautological;
  the blind replay (an LLM following the prose, grader held out) is the real behavioral proof.
- **Injection resistance is dominated by model strength, not skill prose.** Opus resists
  every vector (hidden/visible/subtle/agentic) wrapped or not; Haiku obeys embedded directives
  in synthesis regardless of wrapping. Principle 8 is redundant for strong models and
  ineffective for weak ones — no efficacy band.
- **CSS concealment is irrelevant to an LLM** (it ingests raw markup; hiding only evades
  humans) — the strip-list was theater. The real injection harm is in the **agentic fetch
  loop** (one Haiku native agent fetched the exfil URL + leaked the question), not
  synthesis-text token-append.
- **`canary.sh` grades by token *presence*** → false-positives any transparent report that
  names the token while refusing; needs obeyed-position detection.

## What Didn't Work
- #4's original "over-kill ↓" framing — measured false twice (n=2).
- Majority-keep voting rule — reduces over-kill only by destroying orphan recall.
- The dr-diverse-verify worker's structural "56%→0%" self-check was tautological; the blind
  replay was needed to test behavior.
- Principle 8 fetch-sanitization (delimiter-wrapping + strip-list) — **ditched**: 0 measured
  injection reduction in any tested context. The strip-list CSS-hardening follow-up was
  rejected as wrong-layer (CSS hiding is invisible-to-humans, not to an LLM).

## Next
- Tier-3 `deep-research-native-overlap` — resolve rpm:deep-research vs native bundled overlap.
- Then Tier-4 `audit-workflow-fastpath`; injection follow-ups (`canary-grader-obey-fix`,
  `injection-weak-model-arm`).
- The deep-research changes (`997c530`, `3dec022`, + this session's Principle-8 removal) are
  in-tree/unreleased — a `publish-all.sh` release is a separate explicit step when ready.
